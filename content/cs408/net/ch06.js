window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/net/ch06'] = {

lessons: {

'6-1': {
title: 'DNS 与 FTP',
secs: [
['why', '为什么学这一节',
`<p>域名解析的层次体系（迭代 vs 递归查询）与 FTP 的双连接（控制/数据）模型是本章选择题的固定供应源。</p>`],
['think', '直观理解',
`<p><b>DNS 域名树：</b>根 → 顶级域 → 权威域 → 主机名。主机通常把查询交给本地递归解析器；解析器若无缓存，再向根、顶级域和权威服务器逐步迭代查询。</p>
<blockquote><b>DNS 通常使用 53 端口：</b>普通查询常用 UDP；响应过大、截断后重试以及区域传送等场景可用 TCP。缓存记录受 TTL 约束。<br>
<b>FTP：</b>控制连接使用 TCP 21 并在会话期间保持；数据传输另建 TCP 连接。主动模式下服务器的数据连接通常从端口 20 发起，被动模式下服务器告知客户端一个临时端口，因此不能把所有 FTP 数据连接都固定为端口 20。</blockquote>`],
['def', '要点',
`<p><b>① 递归查询：</b>替你问到底；<b>迭代查询：</b>"我不知道，你去问 X"（本地服务器对根一般用迭代）。</p>
<p><b>② 资源记录：</b>A（IP）、CNAME（别名）、MX（邮件）、NS。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（辨析）主机向本地域名服务器发出的查询通常是？</div>
<p><b>递归查询</b>（主机图省事）；本地域名服务器向根服务器通常<b>迭代查询</b>。</p></div>`],
['warn', '易错点',
`<p>• FTP 控制连接在会话期间保持；数据连接按目录列表或文件传输任务另行建立，主动和被动模式的端口规则不同。</p>
<p>• DNS 既用 UDP 也用 TCP（区域传送）——"只用 UDP"是错误说法。</p>`]
],
quiz: [
{id:'q1', q:'FTP 的控制连接使用端口？', opts:['20', '21', '80', '25'], ans:1, exp:'21 控制、20 数据（主动模式）。'}
]

},

'6-2': {
title: '电子邮件：SMTP、POP3 与 IMAP',
secs: [
['why', '为什么学这一节',
`<p>邮件收发"推拉模型"：SMTP 推送、POP3/IMAP 拉取。三协议的端口与方向是固定选择题。</p>`],
['think', '直观理解',
`<p><b>邮件传递：</b>发件人用户代理把邮件提交给发方邮件服务器，邮件服务器之间通常用 SMTP 中继，收件人再用 POP3、IMAP 或 Web 接口读取。</p>
<blockquote><b>SMTP：</b>基于 TCP，服务器间中继常用 25 端口；邮件提交常用 587 等端口。SMTP 原始邮件体以受限文本格式传送，<b>MIME</b> 通过内容类型和传输编码支持非 ASCII 文本、附件与多媒体。<br>
<b>POP3：</b>默认 TCP 110，偏向下载邮件；是否删除服务器副本由会话命令和客户端策略决定。<b>IMAP：</b>默认 TCP 143，支持在服务器上管理文件夹、状态和多端同步。网页邮箱的浏览器一侧通常用 HTTP(S)，邮件服务器之间仍可用 SMTP。</blockquote>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（方向判断）用户代理从邮件服务器读取邮件使用的协议是？</div>
<p><b>POP3 或 IMAP</b>（拉）；发送用的是 SMTP（推）。</p></div>`],
['warn', '易错点',
`<p>• SMTP "推"、POP3/IMAP "拉"——方向反了就错。</p>
<p>• MIME 不是传输协议，是"格式扩展"让 SMTP 能带附件。</p>`]
],
quiz: [
{id:'q1', q:'SMTP 的默认端口是？', opts:['110', '143', '25', '587'], ans:2, exp:'25（提交也可能 587，教材默认 25）。'}
]

},

'6-3': {
title: 'WWW 与 HTTP',
secs: [
['why', '为什么学这一节',
`<p>HTTP 是应用层之王：<b>1.0 非持久 vs 1.1 持久连接</b>的请求次数计算、报文结构、Cookie/Session、代理缓存——大题小题双料高频。</p>`],
['think', '直观理解',
`<p><b>HTTP：</b>应用层请求—响应协议。HTTP/1.x 和 HTTP/2 通常运行在 TCP 上，HTTP/3 运行在基于 UDP 的 QUIC 上；HTTP 本身不维护应用会话状态，Cookie 等机制可帮助应用关联多次请求。</p>
<blockquote><b>HTTP/1.0 非持久连接：</b>在常见简化模型中每个对象分别建立 TCP 连接；<b>HTTP/1.1</b> 默认使用持久连接，一条连接可传多个请求。HTTP/1.1 可流水化发送请求，但响应仍按请求顺序返回，现实浏览器已较少使用该机制。</blockquote>
<p><b>报文：</b>请求由请求行、首部、空行和可选消息体组成；响应由状态行、首部、空行和可选消息体组成。GET、HEAD 等方法的具体语义由标准定义，状态码 2xx、3xx、4xx、5xx 分别表示不同类别结果。</p>
<p><b>Web 缓存：</b>命中时可减少源站流量和时延；缓存可用 ETag/If-None-Match 或 Last-Modified/If-Modified-Since 向源站重新验证。</p>`],
['def', '计算模板',
`<p><b>简化 RTT 模型：</b>一个 HTML 引用 5 个小图片，客户端到服务器的 RTT=10ms，忽略 DNS、对象传输时间和并行连接。</p>
<p>• HTTP/1.0 非持久且逐个连接：每个对象需 1RTT 建连 + 1RTT 请求响应，6 个对象共 <b>12RTT=120ms</b>；</p>
<p>• HTTP/1.1 持久、非流水：首个 HTML 需 2RTT，其后 5 个图片各需 1RTT，共 <b>7RTT=70ms</b>；</p>
<p>• HTTP/1.1 持久流水：首个 HTML 2RTT，随后把 5 个请求连续发出并在约 1RTT 后收到响应，共 <b>3RTT=30ms</b>。</p>
<p class="muted">只要计入传输时间、服务器处理、TCP/TLS 握手差异或浏览器并发连接，上述数值就要重算。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（方法辨析）只请求文档首部（测试链接有效性）用哪个方法？</div>
<p><b>HEAD</b>——响应不带实体，省带宽。</p></div>`],
['warn', '易错点',
`<p>• HTTP 无状态但可用 Cookie 记住用户；HTTPS = HTTP over TLS（443）。</p>
<p>• 持久连接的"流水线"要求响应按序返回。</p>`]
],
quiz: [
{id:'q1', q:'HTTP/1.1 相比 1.0 的关键改进是？', opts:['改用 UDP', '默认持久连接', '加密', '无报文头'], ans:1, exp:'一条 TCP 传多个对象，减少建立开销。'},
{id:'q2', q:'状态码 404 表示？', opts:['服务器内部错误', '请求成功', '资源未找到', '重定向'], ans:2, exp:'4xx = 客户端侧错误。'}
]

}

}
};
