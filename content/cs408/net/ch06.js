window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/net/ch06'] = {

lessons: {

'6-1': {
title: 'DNS 与 FTP',
secs: [
['why', '为什么学这一节',
`<p>域名解析的层次体系（迭代 vs 递归查询）与 FTP 的双连接（控制/数据）模型是本章选择题的固定供应源。</p>`],
['think', '直观理解',
`<p><b>DNS 域名树：</b>根 → 顶级域（.com/.cn）→ 权限域（example.com）→ 主机。解析顺序：本地 hosts → <b>本地域名服务器（递归查询）</b> → 根/顶级/权限（<b>迭代查询</b>）。</p>
<blockquote><b>DNS 用 UDP 53</b>（主从服务器间区域传送用 TCP）；<b>高速缓存</b>加速。<br>
<b>FTP：</b><b>两条 TCP 连接</b>——控制连接 21（全程保持）+ 数据连接 20（按需建立）；主动模式服务器主动连数据口、被动模式客户端连。</blockquote>`],
['def', '要点',
`<p><b>① 递归查询：</b>替你问到底；<b>迭代查询：</b>"我不知道，你去问 X"（本地服务器对根一般用迭代）。</p>
<p><b>② 资源记录：</b>A（IP）、CNAME（别名）、MX（邮件）、NS。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（辨析）主机向本地域名服务器发出的查询通常是？</div>
<p><b>递归查询</b>（主机图省事）；本地域名服务器向根服务器通常<b>迭代查询</b>。</p></div>`],
['warn', '易错点',
`<p>• FTP 控制连接在整个会话保持，数据连接每传一个文件建一次。</p>
<p>• DNS 既用 UDP 也用 TCP（区域传送）——"只用 UDP"是错误说法。</p>`]
],
quiz: [
{id:'q1', q:'FTP 的控制连接使用端口？', opts:['20', '21', '80', '25'], ans:1, exp:'21 控制、20 数据（主动模式）。'}
]

},

'6-2': {
title: '电子邮件：SMTP、POP3 与 IMAP',
secs: [
['why', '为什么学这一节',
`<p>邮件收发"推拉模型"：SMTP 推送、POP3/IMAP 拉取。三协议的端口与方向是固定选择题。</p>`],
['think', '直观理解',
`<p><b>邮件旅程：</b>发件人 UA → <b>SMTP</b> → 发方服务器 → <b>SMTP</b> → 收方服务器 → <b>POP3/IMAP</b> → 收件人 UA。</p>
<blockquote><b>SMTP（25 端口，TCP）：</b>只能推 ASCII 文本（MIME 扩展多媒体）；<b>POP3（110）：</b>下载到本地（可删服务器副本）；<b>IMAP（143）：</b>在线操作服务器邮箱（多端同步）。<br>
<b>基于万维网的电子邮件</b>（网页邮箱）：两端都用 HTTP，中间服务器之间可能仍 SMTP。</blockquote>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（方向判断）用户代理从邮件服务器读取邮件使用的协议是？</div>
<p><b>POP3 或 IMAP</b>（拉）；发送用的是 SMTP（推）。</p></div>`],
['warn', '易错点',
`<p>• SMTP "推"、POP3/IMAP "拉"——方向反了就错。</p>
<p>• MIME 不是传输协议，是"格式扩展"让 SMTP 能带附件。</p>`]
],
quiz: [
{id:'q1', q:'SMTP 的默认端口是？', opts:['110', '143', '25', '587'], ans:2, exp:'25（提交也可能 587，教材默认 25）。'}
]

},

'6-3': {
title: 'WWW 与 HTTP',
secs: [
['why', '为什么学这一节',
`<p>HTTP 是应用层之王：<b>1.0 非持久 vs 1.1 持久连接</b>的请求次数计算、报文结构、Cookie/Session、代理缓存——大题小题双料高频。</p>`],
['think', '直观理解',
`<p><b>HTTP 特点：</b>基于 TCP 80、无状态（Cookie 补状态）、灵活（可传任意类型）。</p>
<blockquote><b>HTTP/1.0 非持久：</b>每个对象建一次 TCP（2×RTT/对象）；<b>1.1 持久连接：</b>一条 TCP 传多个对象——非流水线（逐个等）vs <b>流水线方式</b>（连发请求），总时间 ≈ 2RTT + n×传输。</blockquote>
<p><b>报文：</b>请求行（方法 GET/POST/HEAD/PUT/DELETE）+ 首部 + 实体；响应：状态码（2xx 成功、3xx 重定向、4xx 客户端错 404、5xx 服务器错）。</p>
<p><b>代理服务器（Web 缓存）：</b>代表源服务器响应——命中则省时延；条件 GET（If-Modified-Since）验证新鲜度。</p>`],
['def', '计算模板',
`<p><b>例：页面 1 个 HTML（引用 5 个图片对象），RTT=10ms、忽略传输时间：</b></p>
<p>• HTTP/1.0 非持久：每对象 2RTT → (1+5)×2×10 = <b>120ms</b>；</p>
<p>• 1.1 持久非流水线：2RTT 建立+首页 + 5×(1RTT+传输) = 2×10+5×10 = <b>70ms</b>；</p>
<p>• 1.1 流水线：2RTT + 1RTT = <b>30ms</b>。</p>
<p class="muted">"TCP 建立需 1RTT + 请求响应 1RTT = 首对象 2RTT"是基准套路。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（方法辨析）只请求文档首部（测试链接有效性）用哪个方法？</div>
<p><b>HEAD</b>——响应不带实体，省带宽。</p></div>`],
['warn', '易错点',
`<p>• HTTP 无状态但可用 Cookie 记住用户；HTTPS = HTTP over TLS（443）。</p>
<p>• 持久连接的"流水线"要求响应按序返回。</p>`]
],
quiz: [
{id:'q1', q:'HTTP/1.1 相比 1.0 的关键改进是？', opts:['改用 UDP', '默认持久连接', '加密', '无报文头'], ans:1, exp:'一条 TCP 传多个对象，减少建立开销。'},
{id:'q2', q:'状态码 404 表示？', opts:['服务器内部错误', '请求成功', '资源未找到', '重定向'], ans:2, exp:'4xx = 客户端侧错误。'}
]

}

}
};
