/* ===== 408 计算机网络 · 第5章 传输层 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/net/ch05'] = {

lessons: {

'5-1': {
title: 'UDP 与 TCP：特点与首部格式',
secs: [
['why', '为什么学这一节',
`<p>传输层双雄。UDP 简单无连接、TCP 可靠面向连接；两者首部字段（尤其 TCP 的六大标志位与序号确认号）是一切 TCP 大题的基础。</p>`],
['think', '直观理解',
`<p><b>UDP：</b>无连接、尽最大努力交付并保留应用报文边界；首部固定 8B，由源端口、目的端口、长度和校验和组成。它可用于 DNS、实时媒体等，但应用若需要可靠性须自行实现。</p>
<blockquote><b>TCP 首部 20～60B：</b>序号 seq 标识本段数据第一个字节的序号；ACK=1 时确认号 ack 表示期望收到的下一个字节序号；此外还有数据偏移、URG/ACK/PSH/RST/SYN/FIN、接收窗口和校验和等字段。TCP 提供面向字节流的可靠传输，不保留应用写入边界。</blockquote>
<p><b>端口：</b>端口号字段为 16 bit，范围 0～65535；0～1023 通常为系统或熟知端口，例如 HTTP 80、DNS 53、SMTP 25。通信端点常用“IP 地址+端口号”表示。</p>
<p><b>校验和：</b>TCP 和 UDP 都把 IP 层信息构成的伪首部纳入计算。TCP 校验和必须使用；UDP 在 IPv4 中校验和值为 0 可表示未使用，但在 IPv6 中通常是强制的。</p>`],
['def', '要点',
`<p><b>① TCP 面向字节流 vs UDP 面向报文</b>：TCP 会把应用数据"拆包合包"，接收方不保证一次 read 对应一次 write。</p>
<p><b>② ACK 标志=1 时确认号才有效</b>；SYN 消耗一个序号（SYN=1 的段 seq=x，确认号=x+1）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（首部计算）TCP 报文段首部固定 20B + 选项 8B，数据偏移字段值？</div>
<p>首部长度 28B，数据偏移单位 4B → 28/4 = <b>7</b>。</p></div>`],
['warn', '易错点',
`<p>• 确认号 ack = "已正确收到的最后一个字节的序号 + 1"（期望的下一个）。</p>
<p>• UDP 长度字段含首部 8B；TCP 无长度字段（靠 IP 层总长推算）。</p>`]
],
quiz: [
{id:'q1', q:'UDP 首部的长度是？', opts:['8 字节', '20 字节', '12 字节', '可变'], ans:0, exp:'四字段×2B = 8B。'},
{id:'q2', q:'在 ACK 标志为 1 且没有使用选择确认时，TCP 报文段中“确认号 = 501”表示？', opts:['恰好已经收到 501 字节', '下一步期望收到序号 501 起的字节', '发送方必须从 501 重传', '接收窗口大小为 501'], ans:1, exp:'TCP 累积确认号表示期望的下一个字节序号；仅凭 ack=501 不能推出总共收到了多少字节，因为初始序号未给出。'}
]

},

'5-2': {
title: 'TCP 连接管理：三次握手与四次挥手',
secs: [
['why', '为什么学这一节',
`<p>全卷最著名的"三次握手"。状态转换（SYN_SENT/ESTABLISHED/TIME_WAIT）、每一步的序号变化、"为什么是三次/四次"的原理阐述是简答与选择双料考点。</p>`],
['think', '直观理解',
`<p><b>三次握手（建立）：</b></p>
<blockquote>① 客户端 → SYN=1, seq=x（进入 SYN_SENT）；<br>② 服务器 → SYN=1, ACK=1, seq=y, ack=x+1（SYN_RCVD）；<br>③ 客户端 → ACK=1, seq=x+1, ack=y+1（双方 ESTABLISHED）。</blockquote>
<p><b>三次握手的作用：</b>双方交换初始序号并确认彼此的发送、接收路径可用，同时协商 TCP 选项。第三次确认能让服务器确认客户端已收到自己的 SYN，避免仅凭单向的旧 SYN 就把连接当作已完全建立。</p>
<p><b>四次挥手（释放）：</b></p>
<blockquote>① 客户端 FIN（FIN_WAIT_1）；② 服务器 ACK（CLOSE_WAIT→客户端 FIN_WAIT_2）——此时服务器还能发数据（半关闭）；③ 服务器数据发完 → FIN（LAST_ACK）；④ 客户端 ACK（<b>TIME_WAIT 等 2MSL</b>）→ CLOSED。</blockquote>
<p><b>为什么四次？</b>TCP 全双工，两个方向各关一次；②③可合并（但服务器可能还有数据）。<b>TIME_WAIT 等 2MSL：</b>保证最后的 ACK 可达 + 旧报文自然消亡。</p>`],
['def', '序号追踪',
`<p><b>例：客户端 SYN seq=100，则服务器确认 ack=101；若握手完成客户端发 100 字节数据 seq=101~200，服务器 ack=201。</b></p>
<p>SYN 与 FIN 各消耗一个序号；纯 ACK 不消耗。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（判断）TIME_WAIT 状态出现在主动关闭方，对吗？</div>
<p><b>对</b>——主动关者最后发 ACK 后等 2MSL，防 ACK 丢失（服务器重传 FIN 时还能再确认）。</p></div>`],
['warn', '易错点',
`<p>• 第三次握手可以捎带数据；第二次握手的 ACK 与 SYN 同段。</p>
<p>• "服务器收到 FIN 后立即发 FIN"错——可能还有数据（这就是四次而非三次挥手的原因）。</p>`]
],
quiz: [
{id:'q1', q:'TCP 建立连接时，第二次握手的报文标志是？', opts:['SYN=1', 'SYN=1, ACK=1', 'ACK=1, FIN=1', '仅 ACK=1'], ans:1, exp:'服务器同时确认（ack=x+1）并同步（seq=y）。'},
{id:'q2', q:'TIME_WAIT 需要等待的时间是？', opts:['1 个 MSL', '2 个 MSL', '1 个 RTT', '无限期'], ans:1, exp:'2MSL：一去一回的最大报文生存期。'}
]

},

'5-3': {
title: 'TCP 可靠传输、流量控制与拥塞控制',
secs: [
['why', '为什么学这一节',
`<p>408 计算题之王：<b>拥塞窗口演化（慢开始/拥塞避免/快重传/快恢复）</b>给场景画 cwnd 曲线几乎年年出；流量控制与滑动窗口结合也常出计算。</p>`],
['think', '直观理解',
`<p><b>可靠传输：</b>TCP 使用字节序号、累积确认、校验和、重传计时器，并可通过重复 ACK 触发快速重传；“3 个重复 ACK”通常指在原确认之外又收到 3 个相同确认。</p>
<blockquote><b>流量控制：</b>接收方用 rwnd 通告可用接收缓存。发送方可发送的数据还受拥塞窗口 cwnd 约束，常用上界为 $\\min(rwnd,cwnd)$；实际在途数据量、应用可用数据和实现规则还会进一步限制。接收窗口为 0 时可用持续计时器发送窗口探测，防止窗口更新丢失造成永久等待。</blockquote>
<p><b>拥塞控制必须说明算法版本和计数口径：</b></p>
<p>• 在经典按 MSS 计数的教学模型中，慢开始让 cwnd 每收到一轮 ACK 近似翻倍，达到 ssthresh 后进入拥塞避免；</p>
<p>• 拥塞避免通常使 cwnd 每 RTT 约增加 1 MSS；</p>
<p>• 超时常令 ssthresh 取丢包前 FlightSize 或 cwnd 的一半附近，并把 cwnd 降到初始窗口；</p>
<p>• 3 个重复 ACK 会触发快速重传。Reno 的快速恢复会先按重复 ACK 临时膨胀 cwnd，收到对重传数据的新确认后再降到 ssthresh；若题目采用简化模型，才可直接写“cwnd=ssthresh”。</p>`],
['def', '经典计算模板',
`<p><b>简化教学模型：</b>设初始 cwnd=1 MSS、ssthresh=8 MSS，并规定每轮结束更新一次窗口。第 0 轮发送前 cwnd=1；经过第 1、2、3 轮后依次变为 2、4、8，随后进入拥塞避免；第 4、5 轮后依次为 <b>9、10</b> MSS。</p>
<p><b>超时示例：</b>若按题设简化为 cwnd=12 时超时，令 ssthresh=6、cwnd=1，则随后每轮为 2、4、6，再线性变为 7、8。实际标准与实现可能按 FlightSize、初始窗口和字节计数处理，考试必须服从题设。</p>
<p class="muted">画图时先注明横轴标的是“第几轮开始”还是“第几轮结束”，否则同一曲线会出现一轮的读数偏差。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（快重传/快恢复）发送方在 cwnd=16 MSS 时收到 3 个重复 ACK，题目采用简化 Reno 模型，如何更新？</div>
<p>立即重传推测丢失的报文段，并把 ssthresh 设为 8 MSS。若题目明确采用简化口径，可记为 cwnd 随后回到 <b>8 MSS</b> 并进入拥塞避免；标准 Reno 的过程还会在恢复期间把 cwnd 临时设为 ssthresh+3 MSS，并在收到新 ACK 后再降到 ssthresh。</p></div>`],
['warn', '易错点',
`<p>• 慢开始"翻倍"到 ssthresh 就停，不是一直翻倍。</p>
<p>• 发送窗口同时受 rwnd、cwnd 和当前已确认范围限制；“取 min”是窗口上限的简化表达，不代表发送方一定有这么多应用数据可发。</p>
<p>• 超时重传的 RTTs 用加权平均（RTTs = 旧值×(1-α)+新RTT×α）。</p>`]
],
quiz: [
{id:'q1', q:'慢开始阶段 cwnd 的增长规律是？', opts:['每 RTT 加 1', '每 RTT 翻倍', '每 RTT 加倍减半', '不变'], ans:1, exp:'指数增长直至 ssthresh。'},
{id:'q2', q:'发送方的发送窗口等于？', opts:['rwnd', 'cwnd', 'min(rwnd, cwnd)', 'max(rwnd, cwnd)'], ans:2, exp:'两个"阀门"取小者。'}
]

}

}
};
