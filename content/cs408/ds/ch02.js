/* ===== 408 数据结构 · 第2章 栈、队列和数组 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/ds/ch02'] = {

lessons: {

'2-1': {
title: '栈',
secs: [
['why', '为什么学这一节',
`<p>栈是"后进先出 LIFO"的限制性线性表。408 每年必考<b>出栈序列合法性判断</b>（卡特兰数），函数调用、表达式求值、递归转非递归全靠它。</p>`],
['think', '直观理解',
`<p><b>栈 = 叠盘子：</b>只能从顶上放（push）、从顶上拿（pop）。最先放的盘子最后才能拿到。</p>
<blockquote><b>n 个元素依次入栈的合法出栈序列数 = 卡特兰数 C(2n,n)/(n+1)。</b>n=3 时有 5 种：任意 3、1、2 的排列中，"3 1 2"非法（3 先出说明 1、2 已在栈中且 2 在 1 上，2 必须先于 1 出）。</blockquote>
<p><b>判断法（手动模拟）：</b>依次入栈，每入一个就尽量出栈；模拟目标序列——能对上就合法。</p>`],
['def', '栈的结构与操作',
`<p><b>① 顺序栈：</b></p>
<pre class="code">#define MaxSize 50
typedef struct {
    int data[MaxSize];
    int top;            /* 栈顶指针 */
} SqStack;
/* 进栈：s-&gt;top++; s-&gt;data[s-&gt;top] = x;  先移指针再存 */
/* 出栈：x = s-&gt;top elem; s-&gt;top--;      先取再退 */</pre>
<p>判空 top == -1（0 号起）；判满 top == MaxSize-1。</p>
<p><b>② 共享栈：</b>两个栈共用一个数组，栈顶设在两端、相向生长——判满 <code>top1 + 1 == top2</code>。空间利用率翻倍的考点。</p>
<p><b>③ 链栈：</b>头插法的单链表，top 即头指针，无栈满问题。</p>
<p><b>④ 应用：</b>括号匹配、表达式求值（中缀转后缀）、函数调用栈、递归、进制转换。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（合法序列判断）入栈序列 1,2,3,4，下列哪个<b>不能</b>是出栈序列？</div>
<p>A. 4 3 2 1（全进全出 ✓）　B. 1 2 3 4（进一个出一个 ✓）　C. 3 1 4 2（模拟：1,2,3 进→出 3；想出 1 但 2 挡着 → <b>非法</b>）　D. 2 4 3 1 ✓</p>
<p class="muted">模拟到"想要的元素被压着"即可判死。</p></div>
<div class="ex-box"><div class="ex-t">例 2（表达式求值）中缀 A+B*(C-D) 转后缀</div>
<p>后缀（逆波兰）：A B C D - * +。计算时遇运算符就弹两个操作数——栈的天然应用。</p></div>`],
['warn', '易错点',
`<p>• 卡特兰数：n 个元素的合法序列数，考过 n=4 → 14 种、n=5 → 42 种。</p>
<p>• 栈顶指针初始值 -1 与 0 的约定不同，进栈出栈语句随之不同，看清题目约定。</p>
<p>• "先进后出"不是"乱序进乱序出"——栈内元素相对顺序永远不变。</p>`]
],
quiz: [
{id:'q1', q:'3 个元素入栈，可能的出栈序列共有几种？', opts:['3', '4', '5', '6'], ans:2, exp:'卡特兰数 C(6,3)/4 = 5 种。'},
{id:'q2', q:'入栈序列 1,2,3,4，出栈序列 3,1,4,2 是否合法？', opts:['合法', '非法', '无法判断', '仅当栈够大时合法'], ans:1, exp:'3 出栈时 1、2 已在栈中，2 在 1 之上，1 不可能先于 2 出。'},
{id:'q3', q:'两个栈共享一个数组空间（共享栈），栈满条件是？', opts:['top1==top2', 'top1+1==top2', 'top1==MaxSize', 'top2==0'], ans:1, exp:'两栈顶相向生长，相邻即满。'}
]

},

'2-2': {
title: '队列',
secs: [
['why', '为什么学这一节',
`<p>队列是"先进先出 FIFO"：排队买饭。BFS 遍历、缓冲区、打印队列、操作系统就绪队列全是它。<b>循环队列的判满判空</b>是 408 顶流考点，没有之一。</p>`],
['think', '直观理解',
`<p><b>队列 = 排队：</b>队尾入队（rear），队头出队（front）。若用普通数组，出队后前部空间浪费——把数组<b>首尾相接成环</b>（下标取模），就是循环队列。</p>
<blockquote><b>循环队列的"牺牲一格"方案：</b>front 指队头，rear 指队尾<b>后一位置</b>：<br>• 入队：<code>rear = (rear+1) % MaxSize</code><br>• 出队：<code>front = (front+1) % MaxSize</code><br>• 队中元素个数：<b>(rear - front + MaxSize) % MaxSize</b><br>• 判满：**(rear+1) % MaxSize == front**（牺牲一格）；判空：rear == front</blockquote>`],
['def', '队列结构与变体',
`<p><b>① 循环队列（牺牲一格法）</b>——上面口诀必背。</p>
<p><b>② 增设 size 变量方案：</b>不牺牲格子，用 size==0 判空、size==MaxSize 判满。</p>
<p><b>③ 链式队列：</b>带头节点单链表 + front/rear 双指针；判空 front==rear（都指头节点）。</p>
<p><b>④ 双端队列 deque：</b>两端都可进出。"输入受限/输出受限双端队列"判断输出序列合法性——模拟即可。</p>
<p><b>⑤ 应用：</b>BFS、OS 就绪队列、打印缓冲区；栈与队列互相实现（两栈实现队列：一个负责进、一个负责出）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（循环队列计算）MaxSize=10，front=3，rear=8，元素个数？</div>
<p>(8 - 3 + 10) % 10 = <b>5</b> 个。</p>
<p>front=8, rear=3 呢？(3-8+10)%10 = <b>5</b>（跨过环尾也正确——取模公式的好处）。</p></div>
<div class="ex-box"><div class="ex-t">例 2（两栈实现队列）</div>
<p>栈 S1 入队、S2 出队：出队时若 S2 空则把 S1 全部倒入 S2。每个元素至多进出各两次，均摊 O(1)。</p></div>`],
['warn', '易错点',
`<p>• 牺牲一格法<b>最多存 MaxSize-1 个元素</b>——问"最多容纳几个"时别答 MaxSize。</p>
<p>• front/rear 指向约定有"指队头元素/指队头前一位置"等变体，公式随之变化，看清题目。</p>
<p>• 循环队列 front 与 rear 谁在谁前都有可能（环形），不能凭大小判断空满。</p>`]
],
quiz: [
{id:'q1', q:'循环队列（牺牲一格）MaxSize=100，front=30，rear=10，元素个数是？', opts:['20', '80', '40', '60'], ans:1, exp:'(10-30+100)%100 = 80。'},
{id:'q2', q:'牺牲一格的循环队列最多存放多少元素？', opts:['MaxSize', 'MaxSize-1', 'MaxSize+1', '任意'], ans:1, exp:'留一格区分队空与队满。'},
{id:'q3', q:'队列的操作特性是？', opts:['先进后出', '先进先出', '随机进出', '只进不出'], ans:1, exp:'FIFO：队尾进、队头出。'}
]

},

'2-3': {
title: '数组与特殊矩阵的压缩存储',
secs: [
['why', '为什么学这一节',
`<p>多维数组本质是"线性表套线性表"，按行/列优先映射公式把二维下标换算成一维地址——考计算。对称/三角/三对角矩阵的压缩存储是固定题型。</p>`],
['think', '直观理解',
`<p><b>二维数组在内存里其实是"拉直的一维"。</b>按行优先（C 语言）：先扫完第一行再第二行。算 a[i][j] 地址 = 起始地址 + (前面共有多少个元素) × 元素大小。</p>
<blockquote><b>行优先：</b>a[i][j] 前有 i 整行（i×n 个）+ 第 i 行的 j 个 → 地址 = LOC + (i×n + j)×size<br>
<b>列优先：</b>先扫列：地址 = LOC + (j×m + i)×size（m 为总行数）</blockquote>
<p><b>对称矩阵压缩：</b>只存下三角（含对角线）到一维数组，n² 压到 n(n+1)/2。</p>`],
['def', '压缩公式速查',
`<p><b>① 对称矩阵下三角按行存 a[i][j]（i≥j，下标从 1 起）：</b></p>
$$ k = \\frac{i(i-1)}{2} + j - 1 \\quad (0 \\le k \\lt \\frac{n(n+1)}{2}) $$
<p class="muted">前 i-1 行共 i(i-1)/2 个，加上本行第 j 个。上三角元素 a[i][j]（i&lt;j）取对称位置 a[j][i]。</p>
<p><b>② 三对角矩阵：</b>仅主对角线及上下相邻共 3n-2 个元素；a[i][j] 映射 k = 2i+j-3（下标从 1 起）。</p>
<p><b>③ 稀疏矩阵：</b>三元组表 (行, 列, 值) 或十字链表存储——考概念。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（地址计算）二维数组 a[0..9][0..19]（10 行 20 列），元素 4 字节，按行优先，a[3][5] 的地址？（首地址 1000）</div>
<p>前面元素个数 = 3×20 + 5 = 65；地址 = 1000 + 65×4 = <b>1260</b>。</p></div>
<div class="ex-box"><div class="ex-t">例 2（对称矩阵）6 阶对称矩阵下三角按行存入一维数组，a[4][2]（i≥j）对应下标 k？</div>
<p>k = 4×3/2 + 2 - 1 = <b>7</b>（下标从 0 起）。</p></div>`],
['warn', '易错点',
`<p>• 下标从 0 还是从 1 起，公式差 1——先看题目约定再套公式。</p>
<p>• 行优先列优先看语言：C 行优先，Fortran 列优先；题目会明说。</p>
<p>• 对称矩阵问 a[i][j]（i&lt;j 上三角）时，先换成 a[j][i] 再套下三角公式。</p>`]
],
quiz: [
{id:'q1', q:'数组 a[0..5][0..7]（6行8列）按行优先，a[2][3] 前共有多少个元素？', opts:['19', '16', '23', '27'], ans:0, exp:'2×8+3 = 19 个。'},
{id:'q2', q:'n 阶对称矩阵压缩存储（只存下三角）需要的单元数是？', opts:['n²', 'n(n+1)/2', 'n(n-1)/2', '3n-2'], ans:1, exp:'下三角含对角线共 n(n+1)/2 个元素。'}
]

}

}
};
