/* ===== 408 计算机组成原理 · 第4章 指令系统 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/co/ch04'] = {

lessons: {

'4-1': {
title: '指令格式与寻址方式',
secs: [
['why', '为什么学这一节',
`<p>指令 = 操作码 + 地址码。<b>寻址方式</b>（数据从哪来、地址怎么算）是本章的核心考点：有效地址计算、相对寻址转移目标计算年年出题。</p>`],
['think', '直观理解',
`<p><b>指令的字长设计：</b>操作码规定操作，地址码或操作数字段说明数据来源。定长格式规整；扩展操作码则让地址字段较少的指令使用更长操作码，提高编码空间利用率。</p>
<blockquote><b>常见寻址方式（括号表示寄存器或存储单元中的内容）：</b><br>立即寻址：形式字段本身就是操作数；直接寻址：EA=A；间接寻址：EA=(A)；寄存器寻址：操作数在 $R_i$ 中；寄存器间接：EA=$(R_i)$；相对寻址：EA=(PC)+A；基址：EA=(BR)+A；变址：EA=(IX)+A；堆栈寻址：栈顶位置通常由 SP 隐含给出。只有需要访问存储器的方式才有通常意义下的有效地址 EA。</blockquote>
<p class="muted">在 408 的典型模型中，基址寄存器内容通常由操作系统或管理程序设置，用于重定位；变址寄存器可由用户程序修改，用于数组访问。具体 ISA 的可见性和修改权限仍以题设为准。</p>`],
['def', '计算模板',
`<p><b>① 相对寻址跳转目标：</b>目标地址 = <b>PC 已加完的值</b> + 偏移 A。注意 PC 在"取指后"已指向下一条指令——考题的坑就在"加不加指令字长"。</p>
<p><b>② 例：</b>指令位于 2000H、长 4B，指令中偏移 A = 100H → 取指后 PC = 2004H → 转移目标 = <b>2104H</b>。</p>
<p><b>③ CISC 与 RISC：</b>传统教材通常把 CISC 概括为指令/寻址方式丰富、编码较灵活、常采用微程序，把 RISC 概括为编码规整、Load/Store、寄存器多且便于流水。它们是设计取向，具体处理器可能混合两者。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（扩展操作码）16 位指令，4 位基本操作码 + 3 个 4 位地址码；若三地址指令 15 条，二地址指令最多几条？</div>
<p>4 位操作码 16 个编码，15 条三地址用掉 15 个，剩 1 个（1111）作扩展前缀：二地址操作码 = 4+4 位，扩展空间 1111 后接 4 位 → 16 条二地址。</p></div>
<div class="ex-box"><div class="ex-t">例 2（寻址辨析）访问数组元素 a[i]（i 存于变址寄存器）适合哪种寻址？</div>
<p><b>变址寻址</b>：EA = (IX)+A，改 IX 即遍历数组——正是变址的招牌用途。</p></div>`],
['warn', '易错点',
`<p>• 相对寻址的基准是"取指后已更新的 PC"，多数题要 +指令字长。</p>
<p>• 间接寻址除取指外，通常还要先访存取得有效地址，再按该地址取操作数；若是多级间接则次数更多。它通常比直接寻址慢，但不能无条件称为所有寻址方式中“最慢”。</p>
<p>• 立即寻址操作数在指令中，取指后无需再访存取数——最快。</p>`]
],
quiz: [
{id:'q1', q:'变址寻址 EA=(IX)+A 的典型用途是？', opts:['程序重定位', '数组循环访问', '立即取常数', '堆栈操作'], ans:1, exp:'变址寄存器程序可改，配合形式地址 A 遍历数组。'},
{id:'q2', q:'指令地址 3000H、长 2 字节、相对寻址偏移 20H，转移目标地址是？', opts:['3020H', '3022H', '301EH', '3024H'], ans:1, exp:'取指后 PC=3002H，目标=3002H+20H=3022H。'}
]

},

'4-2': {
title: 'CISC 与 RISC 的对比',
secs: [
['why', '为什么学这一节',
`<p>x86（CISC）与 ARM/MIPS（RISC）的设计哲学之争。纯对比表格题：指令、寻址、通用寄存器、控制方式、流水线、适合领域六连比。</p>`],
['think', '直观理解',
`<p><b>RISC 的基本思想：让常用指令和数据通路更规整，以利于流水执行和编译优化。</b>复杂操作由多条较简单的指令组合。</p>
<blockquote><b>典型教材对比：</b>RISC 往往采用较规整或定长编码、较少寻址方式、较多通用寄存器和 Load/Store 架构，更适合硬布线与流水线；传统 CISC 往往编码长度和寻址方式更丰富，允许更多指令直接访问存储器，并常大量采用微程序控制。这些是设计倾向，不是所有现代 ISA 都逐条满足的绝对定义。</blockquote>`],
['def', '要点',
`<p><b>① RISC 更能充分利用芯片面积</b>（控制逻辑简单）；<b>② RISC 编译后代码需要优化</b>才能发挥优势；<b>③ RISC 大量使用寄存器-寄存器操作，减少访存</b>。</p>
<p class="muted">现代 x86 外部 CISC、内部翻译成类 RISC 微操作——两种哲学已融合。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（辨析）下列不属于 RISC 特征的是？</div>
<p>A. 指令长度固定　B. 只有 Load/Store 指令访存　C. <b>指令丰富寻址方式多</b>　D. 采用流水线技术 → 选 C（那是 CISC）。</p></div>`],
['warn', '易错点',
`<p>• RISC 不等于"指令一定更少条数完成程序"——指令数可能更多但每条更快更规整。</p>
<p>• "RISC 一定比 CISC 快"是错误表述，要看程序与编译优化。</p>`]
],
quiz: [
{id:'q1', q:'RISC 机器的控制器多采用？', opts:['微程序控制', '组合逻辑（硬布线）控制', '存储逻辑', '人工控制'], ans:1, exp:'指令简单规整 → 硬布线速度优势。'},
{id:'q2', q:'RISC 指令集中访问存储器的指令是？', opts:['全部指令', '只有 Load/Store', '只有算术指令', '没有'], ans:1, exp:'运算只在寄存器间进行，Load/Store 是唯一访存指令。'}
]

}

}
};
