/* ===== 408 计算机组成原理 · 第6章 总线 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/co/ch06'] = {

lessons: {

'6-1': {
title: '总线概述与仲裁方式',
secs: [
['why', '为什么学这一节',
`<p>总线是部件间的"公共高速公路"。考点：总线分类、总线宽度/带宽计算、三种<b>集中式仲裁</b>（链式查询/计数器定时/独立请求）的对比。</p>`],
['think', '直观理解',
`<p><b>总线三分类：</b>数据总线（传数据，双向，位数=机器字长相关）、地址总线（单向 CPU→外，位数决定寻址空间）、控制总线（命令/时序/应答）。</p>
<blockquote><b>总线带宽 = 总线工作频率 × (总线宽度/8)。</b>例：频率 133MHz、宽度 32 位 → 带宽 = 133M × 4B = 532MB/s。</blockquote>
<p><b>集中式仲裁三方式：</b>① <b>链式查询</b>：BG 串行传递，离控制器近的优先级高——结构简单、对故障敏感、优先级固定；② <b>计数器定时查询</b>：计数器轮询——优先级可改变；③ <b>独立请求</b>：每设备一条请求线——速度最快、优先级灵活、线最多。<b>分布式仲裁</b>：无中央仲裁器，各设备自比优先级。</p>`],
['def', '要点',
`<p><b>① 总线特性：</b>机械（插头）、电气（电平）、功能（线的作用）、时间（时序）。</p>
<p><b>② 桥：</b>连接不同总线（如 PCI 桥），实现协议转换与速度隔离——多层次总线结构（CPU-主存总线 / 系统总线 / 高速 / 扩展总线）。</p>
<p><b>③ 主设备/从设备：</b>主设备发起传输获得总线控制权；同一时刻<b>总线上只能有一对主从传输</b>（分时复用）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（带宽计算）总线频率 100MHz、宽度 64 位，每秒最大传输？</div>
<p>100M × 8B = <b>800MB/s</b>。</p></div>
<div class="ex-box"><div class="ex-t">例 2（仲裁）设备离总线控制部件越近优先级越高的方式是？</div>
<p><b>链式查询</b>：总线授权信号 BG 串行链式传递，先到先得。</p></div>`],
['warn', '易错点',
`<p>• 地址总线是<b>单向</b>的（CPU 发出），数据总线双向。</p>
<p>• 链式查询"对故障敏感"：一个设备坏了后面全部申请不到授权。</p>
<p>• 独立请求方式线数最多（每设备 2 根：BR+BG），链式仅 3 根（BR、BG、BS）。</p>`]
],
quiz: [
{id:'q1', q:'总线宽度 32 位、工作频率 66MHz，最大数据传输率是？', opts:['66MB/s', '132MB/s', '264MB/s', '33MB/s'], ans:2, exp:'66M × 4B = 264MB/s。'},
{id:'q2', q:'优先级固定且对电路故障最敏感的集中仲裁方式是？', opts:['独立请求', '计数器查询', '链式查询', '分布式'], ans:2, exp:'链式查询的固有缺点。'}
]

},

'6-2': {
title: '总线操作与定时',
secs: [
['why', '为什么学这一节',
`<p>总线上的"交通规则"：<b>同步定时 vs 异步定时</b>（应答方式），及半同步、分离式事务的概念。选择题一年一小题。</p>`],
['think', '直观理解',
`<p><b>同步定时：</b>统一时钟打拍子，快但强制同步（需各部件速度匹配、总线短）。<b>异步定时：</b>握手应答（不互锁/半互锁/全互锁），慢但灵活、可靠性高。</p>
<blockquote><b>异步三互锁：</b>不互锁（不等应答就撤信号，最快最不可靠）；半互锁（主等从应答）；<b>全互锁</b>（请求-应答-撤销请求-撤销应答全确认，最可靠最慢）。</blockquote>
<p><b>分离式事务：</b>总线申请→主发命令即释放总线→从准备好再申请总线传数据——总线利用率最高（总线上无空闲等待）。</p>
<p><b>总线传输阶段：</b>申请分配 → 寻址 → 传输 → 结束。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（辨析）全互锁方式中，"从设备撤销应答"发生在？</div>
<p>主设备撤销请求信号<b>之后</b>——完整四步握手闭环，任何一步未完成都不进入下一态。</p></div>`],
['warn', '易错点',
`<p>• 同步总线并非不能做异步设备的桥（桥内转换）。</p>
<p>• "分离式"变一次传输为两次总线占用，总线利用率高、系统复杂。</p>`]
],
quiz: [
{id:'q1', q:'可靠性最高的异步通信联系方式是？', opts:['不互锁', '半互锁', '全互锁', '同步'], ans:2, exp:'全互锁完整握手确认，最可靠。'}
]

}

}
};
