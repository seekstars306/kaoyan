/* ===== 408 计算机组成原理 · 第5章 中央处理器 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/co/ch05'] = {

lessons: {

'5-1': {
title: 'CPU 的功能与基本结构',
secs: [
['why', '为什么学这一节',
`<p>CPU = 运算器 + 控制器。本节给出 CPU 内部的"零件清单"：用户可见/不可见寄存器、单/多总线数据通路——为 5-2 的指令执行流程铺路。</p>`],
['think', '直观理解',
`<p><b>控制器两大功能：</b>取指（自动 PC 推进）+ 执行（发微命令序列）。<b>运算器干活，控制器指挥。</b></p>
<blockquote><b>寄存器分类：</b>用户可见：通用寄存器、PSW、PC（部分）；<b>透明（不可见）</b>：MAR、MDR、微指令寄存器、IR（汇编程序员不可直接改）。</blockquote>
<p><b>数据通路三种结构：</b>单总线（简单慢，冲突多）、多总线（并行好）、专用通路。内部总线上的"时分复用"是选择题概念。</p>`],
['def', '要点',
`<p><b>① 专用数据通路</b>性能好但线路复杂；<b>② CPU 内部总线</b>同一时刻只能传一个字。</p>
<p><b>③ 中断系统在 CPU 内的结构：</b>中断允许触发器 EINT、中断标记、中断向量相关部件（第 7 章展开）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（透明性）汇编程序员能直接访问的寄存器是？</div>
<p>通用寄存器、PC（通过转移指令间接影响）、PSW（条件码部分可见）——但 <b>MAR、MDR、IR 不可见</b>。</p></div>`],
['warn', '易错点',
`<p>• "程序可见"与"可修改"不同：PC 可见（影响流程）但用户指令一般不能直接给 PC 赋值。</p>
<p>• 单总线 CPU 中两个操作数不能同时上总线，需要暂存器中转——做题时注意"多了几步"。</p>`]
],
quiz: [
{id:'q1', q:'对汇编程序员完全透明的寄存器是？', opts:['通用寄存器', 'MAR 和 MDR', 'PC', 'PSW'], ans:1, exp:'MAR/MDR 属 CPU 内部工作寄存器，程序员无法也不必访问。'}
]

},

'5-2': {
title: '指令执行过程与数据通路',
secs: [
['why', '为什么学这一节',
`<p>把一条指令的执行拆成"取指 → 间址 → 执行 → 中断"四周期，再拆成微操作序列（如 PC→MAR、M(MAR)→MDR）。大题爱考"写出某指令的取指与执行阶段的微操作序列"。</p>`],
['think', '直观理解',
`<p><b>指令周期四阶段：</b></p>
<blockquote><b>取指周期</b>（所有指令都有）：PC→MAR→主存→MDR→IR，PC+1；<br><b>间址周期</b>（间接寻址才有）：IR 中地址码→MAR→主存→MDR（取有效地址）；<br><b>执行周期</b>（随指令不同）；<b>中断周期</b>（响应中断时：保存断点）。</blockquote>
<p><b>例：ADD (R₀), R₁ 的执行微操作：</b>①R₀→MAR；②M(MAR)→MDR；③MDR→X（暂存）；④(X)+(R₁)→ALU→ACC/MDR→写回。</p>`],
['def', '画图与写序列的规范',
`<p><b>① 数据流描述模板：</b>"源寄存器→总线→目的寄存器""M(MAR)→MDR"格式固定，大题按步给分。</p>
<p><b>② 取指公共部分必写：</b>(PC)→MAR、M(MAR)→MDR、(MDR)→IR、(PC)+1→PC。</p>
<p><b>③ 带标志位运算：</b>ALU 输出同时更新 ZF/OF/CF/SF。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 写出"取指周期"的微操作序列（单总线结构）</div>
<p>① (PC)→MAR；② 1→R（发读命令）；③ M(MAR)→MDR；④ (MDR)→IR；⑤ (PC)+1→PC（或 OP(IR) 译码）。</p>
<p class="muted">每步一个总线传输——单总线结构不能合并两步。</p></div>`],
['warn', '易错点',
`<p>• "PC+1"的实际加量 = 指令字长/取指字节数，变长指令按当条长度加。</p>
<p>• 间接寻址的"间址周期"别与执行周期混写。</p>`]
],
quiz: [
{id:'q1', q:'取指周期结束后，PC 的值已指向？', opts:['当前指令', '下一条指令', '上一条指令', '不变'], ans:1, exp:'取指时 PC 自动增量，指向下一条。'},
{id:'q2', q:'间接寻址指令比直接寻址指令的指令周期多出的阶段是？', opts:['取指周期', '间址周期', '中断周期', '译码阶段'], ans:1, exp:'间址周期取出操作数的真实地址。'}
]

},

'5-3': {
title: '控制器：硬布线与微程序',
secs: [
['why', '为什么学这一节',
`<p>控制器怎么"发信号"？两条路线：<b>硬布线（组合逻辑，快难改）</b>与<b>微程序（把微操作编成微指令存控存，慢易改）</b>。微程序控制器的结构与微指令格式是选择题大户。</p>`],
['think', '直观理解',
`<p><b>微程序思想：软件方法实现硬件控制。</b>一条机器指令 → 一段微程序 → 若干条微指令 → 每条微指令发出一组微命令（控制信号）。</p>
<blockquote><b>核心部件：</b>控制存储器 CM（存微程序，ROM）→ <b>CMAR</b>（微地址寄存器）→ <b>CMDR/μIR</b>（微指令寄存器）。微地址形成：下址字段 / 由机器指令操作码映射入口。</blockquote>
<p><b>微指令编码：</b>水平型（一位一控制，并行强、字长长）/ 垂直型（像机器指令，需译码）。<b>后继微地址：</b>增量式（μPC+1）与断定方式（下址字段+测试）。</p>
<p><b>硬布线：</b>组合逻辑直接由"操作码+节拍+状态"生成控制信号，速度快，RISC 首选；改指令要改电路。</p>`],
['def', '对比表',
`<table style="width:100%;border-collapse:collapse;font-size:13px">
<tr style="background:#f1f4f9"><th style="padding:4px;border:1px solid #e3e8f0"></th><th style="padding:4px;border:1px solid #e3e8f0">硬布线</th><th style="padding:4px;border:1px solid #e3e8f0">微程序</th></tr>
<tr><td style="padding:4px;border:1px solid #e3e8f0">实现</td><td style="padding:4px;border:1px solid #e3e8f0">组合逻辑电路</td><td style="padding:4px;border:1px solid #e3e8f0">控制存储器中的微程序</td></tr>
<tr><td style="padding:4px;border:1px solid #e3e8f0">速度</td><td style="padding:4px;border:1px solid #e3e8f0">快</td><td style="padding:4px;border:1px solid #e3e8f0">慢（访控存）</td></tr>
<tr><td style="padding:4px;border:1px solid #e3e8f0">扩展修改</td><td style="padding:4px;border:1px solid #e3e8f0">难</td><td style="padding:4px;border:1px solid #e3e8f0">易（改微码）</td></tr>
<tr><td style="padding:4px;border:1px solid #e3e8f0">应用</td><td style="padding:4px;border:1px solid #e3e8f0">RISC</td><td style="padding:4px;border:1px solid #e3e8f0">CISC</td></tr>
</table>
<p><b>主存与控存：</b>主存放机器指令程序（RAM），控存放微程序（ROM）——"两类存储器"概念题。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 微程序控制器中，一条机器指令对应？</div>
<p>一段<b>微程序</b>（若干条微指令的集合）；微指令中一位对应一个微命令。</p></div>`],
['warn', '易错点',
`<p>• 微命令（最小控制信号）⊂ 微操作；微指令 = 微命令的集合；微程序 = 微指令序列——术语层级别乱。</p>
<p>• 控存用 ROM，断电不丢；CMAR/CMDR 对程序员透明。</p>
<p>• 相容微命令（可同时出现）与互斥微命令（不能同时）——字段直接编码把互斥的放一字段。</p>`]
],
quiz: [
{id:'q1', q:'微程序控制器中，存放微程序的是？', opts:['主存', '控制存储器（ROM）', 'Cache', '通用寄存器'], ans:1, exp:'控存是控制器内部的 ROM。'},
{id:'q2', q:'与硬布线相比，微程序控制的特点是？', opts:['速度更快', '便于修改扩展指令', '只能用于 RISC', '不需要时序'], ans:1, exp:'改微码即改指令系统，灵活性是最大卖点。'}
]

},

'5-4': {
title: '指令流水线',
secs: [
['why', '为什么学这一节',
`<p>"串行改流水"是 CPU 提速的核心思想。五段流水线（IF-ID-EX-MEM-WB）、<b>吞吐率/加速比/效率</b>计算、三类<b>冒险（冲突）及解决</b>是大题与选择题的双料考点。</p>`],
['think', '直观理解',
`<p><b>流水线 = 洗衣店并行作业：</b>洗、烘、熨、叠四道工序，第 2 件衣服在第 1 件烘干时开始洗——吞吐量翻倍（理想 4 倍）。</p>
<blockquote><b>三大数据：</b>吞吐率 = n/(总时间)；加速比 = 串行时间/流水时间；效率 = 时空图有效面积/总面积。<br>
经典公式：n 条指令、k 段、每段 τ：T = (k+n-1)τ；加速比 = nkτ/(k+n-1)τ。</blockquote>
<p><b>三类冒险（hazard）：</b>① <b>结构冒险</b>：部件争用（指令与数据同存一个存储器）→ 分离 Cache；② <b>数据冒险</b>：后指令要用前指令结果 → 转发/旁路、停顿（插入气泡）、编译重排；③ <b>控制冒险</b>：转移指令改变流程 → 分支预测、延迟分支。</p>`],
['def', '计算与判断模板',
`<p><b>① 流水线执行时间：</b>第一条指令走完全程 kτ，之后每 τ 出一条：T = (k+n-1)τ。</p>
<p><b>② 例：5 段流水 12 条指令：T = (5+12-1)τ = 16τ；加速比 = 12×5/16 = 3.75。</p>
<p><b>③ 数据冒险判断：</b>后指令的源寄存器 = 前指令的目的寄存器（RAW 写后读，最常见）；WAW、WAR 在按序流水线中不出现。</p>
<p><b>④ 超标量/超流水：</b>多条流水线并行发射 / 流水段再细分提高主频；动态多发射需硬件调度。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（吞吐率）5 段流水线、时钟 100MHz、连续执行 20 条指令，实际吞吐率？</div>
<p>T = (5+20-1)×10ns = 240ns；吞吐率 = 20/240ns ≈ 8.3×10⁷ 条/s。</p></div>
<div class="ex-box"><div class="ex-t">例 2（冒险识别）I1: add r1,r2,r3；I2: sub r4,r1,r5——冲突类型？</div>
<p>I2 要读 r1，而 r1 是 I1 的目的寄存器 → <b>RAW 数据冒险</b>，可用旁路转发解决（ALU 结果直送下一指令输入）。</p></div>`],
['warn', '易错点',
`<p>• "转移指令后流水线断流"是控制冒险——分支预测失败要<b>清空已取指令</b>（气泡）。</p>
<p>• 结构冒险经典场景：取指与取数同时访存 → 哈佛结构（指令/数据 Cache 分离）解决。</p>
<p>• 流水线加速比上限 = 段数 k（n→∞ 时），实际达不到。</p>`]
],
quiz: [
{id:'q1', q:'5 段流水线执行 n 条指令（各段等时 τ）的总时间是？', opts:['5nτ', '(n+4)τ', '(5n-1)τ', 'nτ'], ans:1, exp:'(k+n-1)τ = (5+n-1)τ = (n+4)τ。'},
{id:'q2', q:'指令与数据存放在同一存储器引起的冒险类型是？', opts:['数据冒险', '结构冒险', '控制冒险', '外设冒险'], ans:1, exp:'资源冲突 = 结构冒险；分离指令/数据 Cache 解决。'}
]

}

}
};
