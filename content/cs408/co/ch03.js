/* ===== 408 计算机组成原理 · 第3章 存储系统 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/co/ch03'] = {

lessons: {

'3-1': {
title: '主存储器：RAM 与 ROM',
secs: [
['why', '为什么学这一节',
`<p>主存的组成、芯片容量计算、交叉存储是本章计算题主力。<b>"给定芯片拼主存"</b>与"低位交叉存取时间"两个套路必须熟练。</p>`],
['think', '直观理解',
`<p><b>存储器分层：</b>寄存器 → Cache → 主存 → 辅存（磁盘/SSD），一般越靠前速度越快、容量越小、单位成本越高。</p>
<blockquote><b>存储芯片容量：</b>若有 k 根片内地址线、每个地址单元输出 w 位，则共有 $2^k$ 个存储字，容量写作 $2^k\\times w$ 位。例：12 根地址线、8 根数据线对应 4K×8 位；还需片选和读写控制等引脚。</blockquote>
<p><b>RAM：</b>SRAM 以双稳态电路存储，速度快、无需刷新，常用于 Cache；DRAM 以电容电荷存储，需刷新，集成度高，常用于主存。<b>非易失存储：</b>掩膜 ROM、PROM、EPROM、EEPROM 与 Flash；Flash 可电擦写，但通常按块擦除，常用于 SSD 和 U 盘。</p>`],
['def', '存储扩展与刷新',
`<p><b>① 芯片扩展：</b>位扩展（同地址并联，扩数据线）、字扩展（片选区分，扩容量）。总芯片数 = (总容量/单芯片容量)。</p>
<p><b>② 例算：</b>用 16K×8 位芯片构成 64K×32 位存储器：需 4 组位扩展（32/8）× 4 组字扩展（64/16）= <b>16 片</b>；片选需要 2 位地址、片内寻址 14 位。</p>
<p><b>③ DRAM 刷新：</b>集中刷新会产生一段集中“死时间”；分散刷新把刷新操作分摊到各存取周期，单个周期变长；异步刷新把各行刷新均匀分散在题目给定的刷新周期内。刷新通常按<b>行</b>进行，具体刷新周期不能无条件写死为 2ms。</p>
<p><b>④ 交叉存储：</b>低位交叉编址（连续地址在不同存储体）→ 流水线并行存取；高位交叉（顺序编址）→ 扩容用。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（低位交叉存取时间）4 体低位交叉、存取周期 100ns、总线传送 25ns，连续读 4 个字需多久？</div>
<p>流水线：T + (4-1)τ = 100 + 75 = <b>175ns</b>（普通顺序需 400ns）。</p></div>
<div class="ex-box"><div class="ex-t">例 2 SRAM 与 DRAM 刷新对象是？</div>
<p>DRAM 按行刷新（电容漏电），SRAM 不需要刷新（触发器保持）。</p></div>`],
['warn', '易错点',
`<p>• Flash（U盘）是 ROM 家族的读写型，<b>读快写慢</b>、按块擦写。</p>
<p>• 交叉存储加速比与体数 m、τ/T 比值相关；体数 ≤ T/τ 时线性加速。</p>
<p>• 位扩展后地址线不变、数据线变宽；字扩展相反。</p>`]
],
quiz: [
{id:'q1', q:'SRAM 存储信息的原理是？', opts:['电容存储电荷', '触发器（双稳态电路）', '磁化方向', '浮栅电荷'], ans:1, exp:'SRAM 用触发器，不漏电不需刷新；DRAM 用电容需刷新。'},
{id:'q2', q:'4 体低位交叉存储器，存取周期 200ns，总线周期 50ns，连续访问 4 个字需？', opts:['800ns', '350ns', '200ns', '650ns'], ans:1, exp:'T+(m-1)τ = 200+150 = 350ns。'}
]

},

'3-2': {
title: '高速缓冲存储器 Cache',
secs: [
['why', '为什么学这一节',
`<p>408 全科出题密度最高的知识点之一：<b>映射方式、命中率、平均访问时间、写策略</b>。大题算 Cache 位结构与命中率，选择考概念辨析，必须全盘吃透。</p>`],
['think', '直观理解',
`<p><b>Cache 基于"局部性原理"：</b>时间局部性（刚用过的还会用——循环）、空间局部性（用过的邻居马上用——数组）。把热数据搬到 CPU 身边的"随身小仓库"。</p>
<blockquote><b>主存块 → Cache 行的三种放置法：</b><br>① <b>直接映射</b>：每个主存块只去唯一一行（行号 = 块号 mod 行数）——简单但易冲突；地址 = 标记 | 行号 | 块内偏移。<br>② <b>全相联</b>：随便放——最灵活，需全比较（CAM），成本高；地址 = 标记 | 偏移。<br>③ <b>组相联</b>：分组、组间直接、组内全相联（2 路/4 路）——折中方案，最常用。</blockquote>
<p><b>替换算法：</b>LRU（最久未用）、FIFO、LFU、随机——直接映射无需替换。</p>
<p><b>写策略：</b>写命中时<b>全写法（write-through，同时写主存）</b>配写缓冲；<b>回写法（write-back，只写 Cache 置脏位，换出时回写）</b>。写不命中：写分配/非写分配。</p>`],
['def', '计算模板',
`<p><b>① 命中率：</b>在指定访问序列中，$H=N_{hit}/N_{access}$。若把 $N_{main}$ 定义为 Cache 未命中后访问主存的次数，才可写成 $N_{hit}/(N_{hit}+N_{main})$。</p>
<p><b>② 平均访问时间：</b>若 Cache 与主存并行启动，常用 $H t_c+(1-H)t_m$；若先查 Cache、未命中再访问主存，则为 $H t_c+(1-H)(t_c+t_m)$。必须服从题目时序定义。</p>
<p><b>③ Cache 总位数：</b>每行包含数据位、标记位、有效位，以及题设需要的脏位和替换状态。例：按字节编址的 16MB 主存需 24 位地址，直接映射 Cache 数据区 1KB、块大小 8B，共 128 行；块内偏移 3 位、行号 7 位、标记 14 位。若只计有效位而不计其他状态，每行为 64+14+1=79 位，共 79×128 位。</p>
<p><b>④ 分块大小与命中率：</b>块太大 → 行数变少冲突升；块太小 → 局部性利用不足，取中庸。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（命中率）程序访问 Cache 2000 次、主存 50 次，命中率？</div>
<p>H = 2000/2050 ≈ <b>97.6%</b>。</p></div>
<div class="ex-box"><div class="ex-t">例 2（平均访问时间）H=95%，t_cache=10ns，t_main=100ns（同时访问方式）</div>
<p>T = 0.95×10 + 0.05×100 = 9.5+5 = <b>14.5ns</b>。</p></div>
<div class="ex-box"><div class="ex-t">例 3（映射判断）主存块号 253（十进制），直接映射到 128 行 Cache 的哪一行？</div>
<p>253 mod 128 = <b>125</b> 行。</p></div>`],
['warn', '易错点',
`<p>• 直接映射的"行内偏移、组号、标记"位数拆分要按位算，别把字节/位搞混。</p>
<p>• 回写法需要<b>脏位</b>（修改位），全写法不需要；替换算法作用在"冲突发生时"。</p>
<p>• Cache 对程序员<b>透明</b>；写回法会导致主存与 Cache 暂时不一致（多核问题的根源）。</p>
<p>• 虚拟 Cache 与物理 Cache 区别考概念即可。</p>`]
],
quiz: [
{id:'q1', q:'直接映射方式中，主存块放入 Cache 行的公式是？', opts:['任意行', '块号 mod Cache行数', '块号 mod 组数（组内任意）', '顺序循环放'], ans:1, exp:'唯一确定：行号 = 主存块号 mod 行数。'},
{id:'q2', q:'2 路组相联、Cache 8 行，共有几个组？', opts:['2', '4', '8', '16'], ans:1, exp:'每组 2 行 → 8/2 = 4 组。'},
{id:'q3', q:'回写法（write-back）中"脏位"的作用是？', opts:['记录访问次数', '标记该行是否被修改过（换出时需回写）', '存储替换信息', '加密标志'], ans:1, exp:'脏=1 说明 Cache 新于主存，换出前必须回写。'}
]

},

'3-3': {
title: '虚拟存储器',
secs: [
['why', '为什么学这一节',
`<p>主存不够用怎么办？把磁盘当"虚拟内存"用：<b>页式虚拟存储 + TLB 加速</b>。与 OS 的分页管理呼应，计组视角考"地址转换流程与 TLB"。</p>`],
['think', '直观理解',
`<p><b>虚拟地址空间 ≠ 物理内存。</b>程序用虚地址（逻辑地址），运行时由 <b>MMU（含页表）</b>翻译成物理地址。</p>
<blockquote><b>页式转换：</b>虚地址 = 虚页号 VPN + 页内偏移；页表把 VPN → 物理页框号，偏移照抄。缺页（不在主存）→ 缺页异常 → OS 调页。</blockquote>
<p><b>TLB（快表）：</b>页表的 Cache（相联存储器），存最近用过的 VPN→PFN 映射。TLB 命中则免访页表，速度飞跃。<b>访问一次数据的真实流程：</b>TLB → （页表）→ Cache → 主存，四级有没有命中决定访存次数。</p>
<p><b>页式 vs 段式：</b>页定长（对程序员透明）、段变长（对程序员可见、利于共享保护）；段页式 = 先段后页。</p>`],
['def', '转换与计算',
`<p><b>① TLB、页表与 Cache：</b>在常见模型中，TLB 命中可直接得到页框号；TLB 未命中但页表项有效时还要访问页表；若页表项无效则发生缺页异常并由操作系统调页。数据访问本身是否再命中 Cache 是另一层问题。计算平均时间时必须根据题目说明 TLB/Cache 是串行还是并行、页表是否在 Cache 中以及各命中率的条件关系建树加权，不能固定成“TLB 命中就只访存一次”。</p>
<p><b>② 页大小与位数：</b>虚地址 32 位、页 4KB（12 位偏移）→ 虚页号 20 位；页表项数 2²⁰ = 1M 项。</p>
<p><b>③ 页表过大怎么办：</b>多级页表（页目录+页表）；TLB 缓解访问速度。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（位数拆分）虚拟地址 32 位、页大小 8KB，虚页号占几位？</div>
<p>8KB = 2¹³ → 偏移 13 位，虚页号 = 32-13 = <b>19 位</b>。</p></div>`],
['warn', '易错点',
`<p>• TLB、页表和 Cache 处理的是不同问题：TLB 缓存地址转换，页表给出虚页到页框的映射与状态，Cache 缓存存储器数据/指令。事件并非简单统计意义上彼此独立，其可同时发生组合由体系结构和访问流程决定。</p>
<p>• 页内偏移在虚实转换中<b>不变</b>，转换的只是页号部分。</p>
<p>• 缺页处理由操作系统完成（计组只管"发异常"），这是计组与 OS 的接壤点。</p>`]
],
quiz: [
{id:'q1', q:'TLB 存放的内容是？', opts:['最近使用的数据', '最近使用的页表项（虚页号→页框号）', 'Cache 标记', '中断向量'], ans:1, exp:'TLB 是页表的缓存，加速虚→实转换。'},
{id:'q2', q:'虚地址 32 位、页大小 4KB，页表项最多多少个？', opts:['4K', '1M', '2M', '16K'], ans:1, exp:'虚页号 20 位 → 2²⁰ = 1M 个页表项。'}
]

}

}
};
