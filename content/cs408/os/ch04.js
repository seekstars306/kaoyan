/* ===== 408 操作系统 · 第4章 文件管理 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/os/ch04'] = {

lessons: {

'4-1': {
title: '文件与目录：逻辑结构与 FCB',
secs: [
['why', '为什么学这一节',
`<p>文件的逻辑结构（顺序/索引）、FCB 与索引结点、目录结构（单级/两级/树形/无环图）与硬链接软链接——概念题稳定供给区。</p>`],
['think', '直观理解',
`<p><b>FCB = 文件的身份证</b>（名、物理位置、权限、大小、时间）。<b>索引结点 inode</b> 把"文件描述"与"目录项"分离：目录项只存 文件名 + inode 号 → 目录瘦身、检索加速。</p>
<blockquote><b>目录结构：</b>树形（便于分类管理，不便于共享）→ 无环图目录（不同名共享：硬链接，inode 引用计数）；<b>软（符号）链接：</b>另建一个"路径名文件"，原文件删除后软链接悬空——硬软链接辨析是选择题。</blockquote>`],
['def', '要点',
`<p><b>① 文件逻辑结构：</b>顺序文件、索引文件、索引顺序文件；记录定长/变长。</p>
<p><b>② 路径检索：</b>从根（绝对路径）或当前目录（相对路径）逐级查目录；"打开文件"= 把 FCB 调入内存打开文件表。</p>
<p><b>③ 硬链接：</b>多个目录项指向同一 inode，不能跨文件系统；软链接：独立 inode 存路径，可跨文件系统。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（辨析）删除被硬链接的文件（引用计数 2）后？</div>
<p>引用计数减 1 → 文件数据仍在，另一链接照常访问；引用归零才真正删除。软链接的目标被删则<b>悬空报错</b>。</p></div>`],
['warn', '易错点',
`<p>• "打开文件"操作是建立 FCB 与内存打开文件表的关联，不是把整个文件读入内存。</p>
<p>• inode 中<b>不含文件名</b>——文件名在目录项里。</p>`]
],
quiz: [
{id:'q1', q:'索引结点（inode）中不包括的信息是？', opts:['文件物理地址', '文件大小', '文件名', '引用计数'], ans:2, exp:'文件名在目录项中，inode 只有 inode 号。'},
{id:'q2', q:'不同目录下的两个文件名指向同一 inode，这是？', opts:['软链接', '硬链接', '复制文件', '快捷方式'], ans:1, exp:'硬链接共享 inode，引用计数 &gt;1。'}
]

},

'4-2': {
title: '文件物理结构与磁盘管理',
secs: [
['why', '为什么学这一节',
`<p>文件在磁盘上怎么放？<b>连续/链接/索引三种物理结构</b>的读写性能对比 + <b>FAT、位示图</b>的空间管理 + 磁盘调度算法（SSTF/SCAN/C-SCAN）计算，是本章大题双雄。</p>`],
['think', '直观理解',
`<p><b>物理结构三流派：</b></p>
<blockquote>① <b>连续分配</b>：文件占连续磁盘块，能直接计算任意逻辑块的位置，但扩展困难并会产生外部碎片；<br>② <b>隐式链接</b>：每个数据块保存下一块指针，定位后部逻辑块时要读取此前各块；<b>显式链接 FAT</b>：把链接指针集中在文件分配表中，若 FAT 已装入或缓存于内存，可在内存中沿链定位而不必读取中间数据块，但定位第 i 块仍需沿表项逐步查找；<br>③ <b>索引分配</b>：索引块保存文件各数据块地址，支持直接访问；大文件可用多级或混合索引。直接块和各级间接块的数量必须按题设，不能把某个 UNIX 示例当作统一规格。</blockquote>
<p><b>磁盘调度：</b><b>SSTF</b> 每次服务距离当前磁头最近的请求，可能使远端请求饥饿；<b>SCAN</b> 沿一个方向移动到题设规定的磁盘端点再反向；<b>LOOK</b> 到该方向最远请求便反向；<b>C-SCAN</b> 只沿一个方向服务，到端点后快速返回另一端。</p>
<p><b>位示图：</b>每个磁盘块用 1 bit 表示占用状态；块号到字号、位号的换算必须先确认编号起点和一个字包含的位数。</p>`],
['def', '混合索引计算模板',
`<p><b>混合索引示例：</b>题设规定块大小 4KB、块地址 4B，所以每个索引块可存 1024 个地址；inode 有 10 个直接地址、1 个一级、1 个二级、1 个三级间接地址。</p>
<p>数据块最大数量为 $10+1024+1024^2+1024^3$，最大文件字节数为该数量乘 4KB，约为 <b>4TiB</b>。这里没有把保存各级索引表本身的块计入“文件数据容量”。</p>
<p><b>问访问逻辑块需几次磁盘 I/O：</b>先判断落在直接、一级、二级还是三级间接范围；若 inode 已在内存而索引块均未缓存，访问数据分别需 1、2、3、4 次磁盘 I/O。缓存条件不同，次数也不同。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（LOOK 与 SCAN）请求为 98,183,37,122,14,124,65,67，当前磁头在 53，先向增大方向；磁道编号为 0～199</div>
<p><b>LOOK</b> 只走到该方向最远请求 183 就回头：53→65→67→98→122→124→183→37→14，总移动 $(183-53)+(183-14)=<b>299</b>$。</p>
<p><b>SCAN</b> 按本题约定要先走到端点 199 再回头：总移动 $(199-53)+(199-14)=<b>331</b>$。若题目对“SCAN 是否必须到物理端点”另有定义，应服从题设。</p></div>`],
['warn', '易错点',
`<p>• FAT 若已装入内存，沿链接表定位目标块不需读取中间数据块，但仍要沿 FAT 表项逐项查找；不要把它误认为可按逻辑块号 O(1) 直接寻址。</p>
<p>• SSTF 可能饥饿（远处请求一直排队）；C-SCAN 对中间磁道更公平。</p>
<p>• 位示图字号从 0 起：块号 n → 字号 ⌊n/字长⌋、位号 n mod 字长。</p>`]
],
quiz: [
{id:'q1', q:'在 FAT 已装入内存的题设下，哪种文件分配方式能在不读取中间数据块的情况下沿表定位目标逻辑块？', opts:['连续分配', '隐式链接', '显式链接（FAT）', '索引分配'], ans:2, exp:'FAT 中的链接表项可在内存中逐项查找，因此无需读出前面的数据块；但定位仍需沿 FAT 链遍历，并非 O(1) 直接寻址。'},
{id:'q2', q:'磁盘调度算法中可能"饥饿"远处请求的是？', opts:['FCFS', 'SSTF', 'SCAN', 'C-SCAN'], ans:1, exp:'SSTF 一味贪近，远端可能长期得不到服务。'}
]

}

}
};
