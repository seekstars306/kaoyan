window.CONTENT = window.CONTENT || {};
/* ===== 408 操作系统 · 第5章 输入输出管理 ===== */
window.CONTENT['cs408/os/ch05'] = {

lessons: {

'5-1': {
title: 'I/O 管理概述与 I/O 核心子系统',
secs: [
['why', '为什么学这一节',
`<p>OS 的 I/O 软件四层（用户层I/O→设备独立性软件→驱动→中断）、<b>设备独立性、SPOOLing 假脱机</b>是概念重点；与计组中断/DMA 呼应。</p>`],
['think', '直观理解',
`<p><b>I/O 软件层次（从上到下）：</b>用户层 I/O 库 → <b>设备独立性软件</b>（逻辑命名、保护、缓冲、分配）→ 设备驱动程序（处理设备细节）→ 中断处理程序 → 硬件。具体系统的分层边界可能不同。</p>
<blockquote><b>设备独立性：</b>程序使用逻辑设备名或统一接口，OS 再映射到可用的物理设备，使替换同类设备时通常无需改应用程序。<br>
<b>SPOOLing 假脱机：</b>借助磁盘上的输入井、输出井和相应队列，把独占设备的使用过程组织成多个进程可提交请求的共享服务；打印队列是典型例子。</blockquote>
<p><b>设备分类：</b>块设备通常按可寻址的数据块传输，如磁盘；字符设备通常按字符流传输，如键盘。分类描述接口特性，不等于所有设备内部实现都相同。</p>`],
['def', '要点',
`<p><b>① 设备分配中的典型数据结构：</b>DCT 设备控制表、COCT 控制器控制表、CHCT 通道控制表，对应“设备—控制器—通道”的层次。</p>
<p><b>② 缓冲公式必须先认清阶段：</b>若设备把一块输入系统缓冲区需 T，把该块复制到用户工作区需 m，CPU 处理工作区需 C，则常见教材模型下单缓冲的稳态周期为 $\\max(T,C)+m$，双缓冲的稳态周期约为 $\\max(T,C+m)$。启动、收尾和各阶段能否重叠都要按题设另计。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（两阶段流水）设备输入一块需 T，CPU 在独立工作区处理一块需 C；交接耗时忽略，连续处理 n 块需要多久？</div>
<p>先输入第 1 块用 T，随后输入下一块可与处理当前块重叠；完成 n 块的总时间为 <b>$T+(n-1)\\max(T,C)+C$</b>。例如 T=5ms、C=3ms、n=4，时间为 5+3×5+3=<b>23ms</b>。</p>
<p class="muted">若 CPU 直接占用同一个缓冲区处理、或交接复制不可忽略，就不能套这个两阶段模型。</p></div>`],
['warn', '易错点',
`<p>• SPOOLing 需要<b>磁盘（输出井）</b>与多个进程的请求队列，不是纯内存技术。</p>
<p>• 驱动程序负责把抽象命令转成设备操作；设备独立性软件不做硬件细节。</p>`]
],
quiz: [
{id:'q1', q:'把独占设备改造成共享设备的 OS 技术是？', opts:['缓冲技术', 'SPOOLing', 'DMA', '虚拟内存'], ans:1, exp:'假脱机：磁盘井 + 队列模拟共享打印机。'}
]

},

'5-2': {
title: '磁盘与磁盘调度算法',
secs: [
['why', '为什么学这一节',
`<p>磁盘的物理构造决定性能公式：<b>存取时间 = 寻道 + 旋转延迟 + 传输</b>；磁盘地址（柱面/盘面/扇区）与性能计算结合出题。</p>`],
['think', '直观理解',
`<p><b>磁盘时间三部分：</b></p>
<blockquote>• <b>寻道时间</b>：磁头移动到目标柱面；<br>• <b>旋转延迟</b>：等待目标扇区转到磁头下。若目标角位置均匀随机，平均为半圈时间 $1/(2r)$；<br>• <b>传输时间</b>：数据经过磁头的时间，连续传输 b 字节时由转速和每磁道容量决定。</blockquote>
<p><b>减少机械开销：</b>先处理同一柱面上的请求可避免再次寻道；切换盘面是否另有开销及磁盘地址字段顺序，以题目给出的控制器模型为准，不能把“柱面→盘面→扇区”当作所有磁盘的唯一编址规定。</p>
<p><b>RAID 概念：</b>RAID 0 只做条带化、无冗余；RAID 1 使用镜像；RAID 5 使用跨磁盘分布的块级奇偶校验，至少需要 3 块盘并可容忍 1 块盘故障。</p>`],
['def', '计算模板',
`<p><b>例：转速 7200rpm = 120r/s，平均旋转延迟 = 1/(2×120)s ≈ 4.17ms。</b></p>
<p><b>访问一个随机扇区：</b>若平均寻道时间为 $T_s$、转速为 r 圈/秒、每磁道有 N 个等长扇区，则平均时间约为 $T_s+1/(2r)+1/(Nr)$。</p>
<p><b>连续读取整条磁道：</b>从随机角位置开始，还要先等目标起始扇区转到磁头下，因此平均约为 $T_s+1/(2r)+1/r$；只有题目说明已位于起始扇区时，才省去平均旋转等待。</p>
<p><b>交替编号/柱面错位：</b>传统磁盘可通过错开逻辑扇区或相邻磁道的起始位置，为控制器处理和切换磁头预留时间；具体是否需要取决于设备。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（综合）转速 6000rpm、每道 200 扇区，平均旋转延迟与读 1 扇区传输时间？</div>
<p>转速 100r/s，转一圈 10ms → 平均旋转延迟 <b>5ms</b>；读 1 扇区 = 10ms/200 = <b>0.05ms</b>。</p></div>`],
['warn', '易错点',
`<p>• 平均旋转延迟是<b>半圈</b>时间，别按整圈算。</p>
<p>• SCAN/C-SCAN 计算注意"是否到磁盘端点才回头"（看题目给不给最大磁道号）。</p>
<p>• 磁盘是<b>块设备</b>、可随机存取；磁带是顺序存取。</p>`]
],
quiz: [
{id:'q1', q:'磁盘存取时间中通常占比最大的是？', opts:['旋转延迟', '寻道时间', '传输时间', '中断时间'], ans:1, exp:'磁头机械移动最慢。'},
{id:'q2', q:'转速 12000rpm 的磁盘平均旋转延迟约是？', opts:['10ms', '5ms', '2.5ms', '0.5ms'], ans:2, exp:'200r/s → 一圈 5ms → 平均半圈 2.5ms。'}
]

}

}
};
