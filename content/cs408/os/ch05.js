window.CONTENT = window.CONTENT || {};
/* ===== 408 操作系统 · 第5章 输入输出管理 ===== */
window.CONTENT['cs408/os/ch05'] = {

lessons: {

'5-1': {
title: 'I/O 管理概述与核心子系统',
secs: [
['why', '为什么学这一节',
`<p>OS 的 I/O 软件四层（用户层I/O→设备独立性软件→驱动→中断）、<b>设备独立性、SPOOLing 假脱机</b>是概念重点；与计组中断/DMA 呼应。</p>`],
['think', '直观理解',
`<p><b>I/O 软件层次（从上到下）：</b>用户层 I/O 库 → <b>设备独立性软件</b>（命名映射、保护、缓冲、分配）→ 设备驱动程序（懂硬件细节）→ 中断处理程序 → 硬件。</p>
<blockquote><b>设备独立性：</b>程序用逻辑设备名（打印机），OS 负责映射到物理设备——换设备不改程序。<br>
<b>SPOOLing 假脱机：</b>用磁盘上的输入/输出井把独占设备（打印机）模拟成共享设备——排队打印的原理，"用空间换共享"。</blockquote>
<p><b>设备分类：</b>块设备（磁盘：可寻址、按块传输）/字符设备（键盘：流式）；低速/中速/高速。</p>`],
['def', '要点',
`<p><b>① 设备分配中的数据结构：</b>DCT 设备表、COCT 控制器表、CHCT 通道表——"设备-控制器-通道"三级。</p>
<p><b>② 单缓冲/双缓冲/循环缓冲/缓冲池：</b>单缓冲处理一块耗时 max(C,M)+m？——公式题（C 设备输入时间、M 处理时间）：单缓冲 max(C,M)+M，双缓冲 max(C, M)（约）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（缓冲计算）设备输入一块需 T，进程处理一块需 M，单缓冲连续处理 n 块的总时间？</div>
<p>约 n×max(T, M) + M（缓冲与处理重叠）。双缓冲则进一步并行化 ≈ n×max(T,M)。</p>
<p class="muted">"输入与处理能否重叠"是缓冲的意义所在。</p></div>`],
['warn', '易错点',
`<p>• SPOOLing 需要<b>磁盘（输出井）</b>与多个进程的请求队列，不是纯内存技术。</p>
<p>• 驱动程序负责把抽象命令转成设备操作；设备独立性软件不做硬件细节。</p>`]
],
quiz: [
{id:'q1', q:'把独占设备改造成共享设备的 OS 技术是？', opts:['缓冲技术', 'SPOOLing', 'DMA', '虚拟内存'], ans:1, exp:'假脱机：磁盘井 + 队列模拟共享打印机。'}
]

},

'5-2': {
title: '磁盘与磁盘调度',
secs: [
['why', '为什么学这一节',
`<p>磁盘的物理构造决定性能公式：<b>存取时间 = 寻道 + 旋转延迟 + 传输</b>；磁盘地址（柱面/盘面/扇区）与性能计算结合出题。</p>`],
['think', '直观理解',
`<p><b>磁盘时间三件套：</b></p>
<blockquote>• <b>寻道时间</b>：磁头移到目标磁道（大头，机械运动）；<br>• <b>旋转延迟</b>：目标扇区转到磁头下 = 半圈时间 = 1/(2×转速)；<br>• <b>传输时间</b>：读写数据本身。</blockquote>
<p><b>读文件顺序建议：</b>按"柱面（磁道）→ 盘面 → 扇区"访问最省寻道（同柱面不同盘面磁头不动）；这也是磁盘地址编址顺序。</p>
<p><b>RAID 概念：</b>RAID0 条带（快无冗余）、RAID1 镜像、RAID5 分布式奇偶校验——可靠性与速度权衡。</p>`],
['def', '计算模板',
`<p><b>例：转速 7200rpm，平均旋转延迟 = 1/(2×120r/s) ≈ 4.17ms。</b></p>
<p><b>完整例（常见大题）：</b>每磁道 100 扇区、转速 r、寻道 s/道：读一个扇区时间 = 寻道 + 1/(2r) + 1/(100r)。连续读同磁道下 100 扇区 = 寻道 + 1/r。</p>
<p><b>交替编号/错位命名：</b>为避免连续扇区"来不及读"而错开编号——考概念。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（综合）转速 6000rpm、每道 200 扇区，平均旋转延迟与读 1 扇区传输时间？</div>
<p>转速 100r/s，转一圈 10ms → 平均旋转延迟 <b>5ms</b>；读 1 扇区 = 10ms/200 = <b>0.05ms</b>。</p></div>`],
['warn', '易错点',
`<p>• 平均旋转延迟是<b>半圈</b>时间，别按整圈算。</p>
<p>• SCAN/C-SCAN 计算注意"是否到磁盘端点才回头"（看题目给不给最大磁道号）。</p>
<p>• 磁盘是<b>块设备</b>、可随机存取；磁带是顺序存取。</p>`]
],
quiz: [
{id:'q1', q:'磁盘存取时间中通常占比最大的是？', opts:['旋转延迟', '寻道时间', '传输时间', '中断时间'], ans:1, exp:'磁头机械移动最慢。'},
{id:'q2', q:'转速 12000rpm 的磁盘平均旋转延迟约是？', opts:['10ms', '5ms', '2.5ms', '0.5ms'], ans:2, exp:'200r/s → 一圈 5ms → 平均半圈 2.5ms。'}
]

}

}
};
