/* ===== 408 操作系统 · 第1章 操作系统概述 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/os/ch01'] = {

lessons: {

'1-1': {
title: '操作系统的概念、特征与功能',
secs: [
['why', '为什么学这一节',
`<p>OS 是硬件之上、应用之下的"总管家"。四大特征（并发/共享/虚拟/异步）是本章的必考骨架，概念题从这里出。</p>`],
['think', '直观理解',
`<p><b>OS 的两个身份：</b>① 管理员（管理处理机、内存、文件、设备四大资源）；② 服务员（给用户和应用提供接口：命令行、GUI、系统调用）。</p>
<blockquote><b>四大特征：</b><b>并发</b>（多个事件在同一时间间隔内推进，单核上交替、多核上还可能并行）、<b>共享</b>（资源供多个并发执行体使用：互斥共享或同时访问）、<b>虚拟</b>（把一个物理实体映射成多个逻辑实体，或扩展其表现容量）、<b>异步</b>（进程以不可预知的速度走走停停，但正确同步的程序仍应得到可接受的结果）。并发与共享互为存在条件，是多道程序环境的总纲。</blockquote>`],
['def', '要点',
`<p><b>① 并发 vs 并行：</b>并行强调多个事件在同一时刻执行，通常需要多个处理单元；并发强调同一时间间隔内都在推进，单核也能实现。</p>
<p><b>② 共享两方式：</b>互斥共享要求一个时间段内只允许一个进程使用，如一台不可同时打印的打印机；同时访问允许多个进程在同一时间段内交替或并行访问，如可并发读写的磁盘文件。</p>
<p><b>③ 虚拟典型：</b>时分复用形成多个逻辑处理器的使用体验；虚拟存储通过地址映射和换入换出扩展进程可见的地址空间。</p>
<p><b>④ 系统调用：</b>系统调用是应用程序主动请求内核服务的编程接口，调用时通过陷入机制进入内核态；库函数可能封装系统调用，但并非每个普通函数都会进入内核。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（辨析）单核 CPU 上运行多个程序是并行还是并发？</div>
<p><b>并发</b>：时间片轮转宏观同时、微观串行。并行需要多核硬件。</p></div>`],
['warn', '易错点',
`<p>• 分时系统特征：同时性、交互性、独立性、及时性；实时系统强调"可靠性+及时性"。</p>
<p>• 系统调用运行在<b>内核态</b>（管态），应用程序运行在<b>用户态</b>（目态）。</p>`]
],
quiz: [
{id:'q1', q:'操作系统四大特征中，最基本的是？', opts:['虚拟', '并发与共享', '异步', '不确定性'], ans:1, exp:'并发与共享互为存在条件，是最基本的两大特征。'},
{id:'q2', q:'用户程序请求 OS 服务的接口是？', opts:['机器指令', '系统调用', '中断向量', '页表'], ans:1, exp:'系统调用（陷入 trap）是用户态进入内核态的合法通道。'}
]

},

'1-2': {
title: '操作系统的发展与分类',
secs: [
['why', '为什么学这一节',
`<p>从手工到多道：每代系统解决上一代的"缺点"，这条主线记住即可通杀选择题。多道批处理的"提高资源利用率"原理是理解后续一切的基础。</p>`],
['think', '直观理解',
`<p><b>演进线：</b>手工（独占）→ 单道批处理（自动顺序，内存仅一道）→ <b>多道批处理</b>（内存多道、宏观并行微观串行、提高利用率；缺点：无交互、周转时间长）→ 分时系统（时间片轮转、交互）→ 实时系统（硬实时：绝对时限；软实时：尽量）→ 网络化分布式化。</p>
<blockquote><b>多道程序设计提高效率的根本原因：</b>CPU 与 I/O 设备<b>并行工作</b>——A 进程等 I/O 时 CPU 切换给 B 干活。</blockquote>`],
['def', '要点',
`<p><b>① 分时系统时间片</b>不能太大（响应差）也不能太小（切换开销大）。</p>
<p><b>② 实时系统</b>：工业控制、导弹制导（硬实时）；视频播放（软实时）。</p>
<p><b>③ 引入多道程序设计所需的关键能力：</b>能在作业等待 I/O 时获知事件并切换执行，例如中断、DMA 或通道等异步 I/O 支持，以及内存保护、处理机调度等操作系统机制。不同历史系统的具体硬件组合并不完全相同。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（单道与多道）A、B 都先使用 CPU 5s，再各做独立 I/O 5s；只有一个 CPU，I/O 设备可与 CPU 并行。若先运行 A，再运行 B，总完成时间？</div>
<p>单道系统要等 A 的 I/O 完成后才运行 B：A 用时 10s，B 再用 10s，共 <b>20s</b>。多道系统可在 A 于 5～10s 做 I/O 时，让 B 同时使用 CPU；随后 B 于 10～15s 做 I/O，因此共 <b>15s</b>。具体重叠量取决于各作业的 CPU/I/O 次序与设备冲突，不能只凭“两个作业”猜时间。</p></div>`],
['warn', '易错点',
`<p>• 多道批处理系统<b>不提供交互</b>——"提高吞吐量与资源利用率"是它的标签。</p>
<p>• 分时系统追求"响应时间"，批处理追求"吞吐量"。</p>`]
],
quiz: [
{id:'q1', q:'多道批处理系统的主要优点是？', opts:['人机交互好', '提高资源利用率和吞吐量', '实时性强', '代码最简单'], ans:1, exp:'内存多道 + CPU/IO 并行 = 利用率提升。'}
]

},

'1-3': {
title: '操作系统的运行环境（内核态与用户态）',
secs: [
['why', '为什么学这一节',
`<p>"两层情绪"：用户态受限、内核态全能。切换时机（系统调用/中断/异常）与"内核包含什么"是概念题高频。</p>`],
['think', '直观理解',
`<p><b>CPU 状态两档：</b><b>用户态</b>（只能执行非特权指令，不能碰硬件资源）与<b>内核态（管态/系统态）</b>（特权指令全开）。</p>
<blockquote><b>特权指令举例：</b>I/O 指令、置时钟、开关中断、修改页表基址寄存器。<b>非特权指令：</b>算术运算、访存读数据。<br>
<b>切换到内核态的三大时机：</b>系统调用（主动陷入）、异常（缺页/除零）、外中断（时钟/I/O）——<b>切换由硬件完成</b>。从内核态回用户态：中断返回指令（如 iret）。</blockquote>`],
['def', '内核组成',
`<p><b>内核（核心态运行的部分）：</b>时钟管理、中断处理、原语（最短不可分割）、系统控制的数据结构及处理（PCB、队列）。</p>
<p><b>大内核/微内核之争：</b>大内核（Linux：性能好、耦合高）vs 微内核（Minix：只留最基本功能在内核，消息传递通信，可靠但频繁切换）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（判断）"除零异常"触发用户态→内核态切换，对吗？</div>
<p><b>对</b>——异常处理程序在内核运行；异常由当前指令产生，是"同步"的。</p></div>`],
['warn', '易错点',
`<p>• "由用户态到内核态的切换"靠<b>中断/异常/系统调用</b>（硬件机制），不是一条普通指令。</p>
<p>• 读取系统时间通常通过系统调用、共享只读页面或体系结构提供的用户态计时指令完成；某条“读时钟”指令是否特权取决于具体体系结构和题设。设置硬件时钟、修改中断允许状态等通常是特权操作。</p>`]
],
quiz: [
{id:'q1', q:'下列指令中属于特权指令的是？', opts:['加法运算', '设置时钟的值', '内存读数', '条件跳转'], ans:1, exp:'触及硬件资源的（置时钟、开关中断、IO）是特权指令。'}
]

}

}
};
