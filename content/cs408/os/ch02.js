window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/os/ch02'] = {

lessons: {

'2-1': {
title: '进程的概念、组成与状态转换',
secs: [
['why', '为什么学这一节',
`<p>进程是 OS 的主角。<b>五状态转换图</b>（创建-就绪-运行-阻塞-终止）是本章第一必背图，"XX 事件引起 XX 转换"年年出题。</p>`],
['think', '直观理解',
`<p><b>进程 = 程序的一次执行过程</b>，通常由 <b>PCB（身份证+档案）</b>、程序段和数据段组成；PCB 是进程存在的唯一标志。</p>
<blockquote><b>五状态转换（核心图）：</b><br>创建 → 就绪（资源基本具备，只等 CPU）→ 运行（正占 CPU）→ 终止；<br>运行 → 就绪（时间片到或被更高优先级进程抢占）；运行 → 阻塞（等待 I/O、资源或事件）；<b>阻塞 → 就绪</b>（等待的事件发生）。<br>两个不能直接发生的转换：<b>阻塞→运行</b>（必须先进入就绪态，再由调度程序选中）和<b>就绪→阻塞</b>（只有正在运行的进程才能执行等待操作）。</blockquote>
<p><b>挂起态（七状态模型）：</b>在常见教材模型中，进程因换出内存而进入就绪挂起或阻塞挂起；“挂起”与“因等待事件而阻塞”是不同维度。</p>`],
['def', '组成与组织',
`<p><b>① PCB 内容：</b>进程描述信息（PID、UID）、控制管理信息（状态、优先级）、资源分配清单、CPU 现场（寄存器快照——切换的凭据）。</p>
<p><b>② PCB 组织：</b>链接方式（就绪队列/阻塞队列多个）与索引方式。</p>
<p><b>③ 原语：</b>创建原语（申请空白 PCB+分配资源+初始化）、撤销原语、阻塞原语 Block（运行→阻塞，插入等待队列）、唤醒原语 Wakeup（阻塞→就绪）。<b>Block 与 Wakeup 成对出现。</b></p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（转换判断）"进程 P 申请打印机未果"引发什么转换？</div>
<p>运行 → <b>阻塞</b>（等待资源，主动行为）。打印机可用时由唤醒原语把它置为<b>就绪</b>（不能直接进运行！）。</p></div>`],
['warn', '易错点',
`<p>• "就绪→阻塞"与"阻塞→运行"不存在——方向图要刻在脑里。</p>
<p>• PCB 全程唯一且与进程共存亡；程序关闭后 PCB 回收。</p>
<p>• 进程创建需多道程序环境；单道批处理无进程并发。</p>`]
],
quiz: [
{id:'q1', q:'下列状态转换不可能发生的是？', opts:['运行→就绪（时间片到）', '阻塞→运行', '运行→阻塞', '就绪→运行'], ans:1, exp:'阻塞进程必须先经调度进入就绪，不能直接上 CPU。'},
{id:'q2', q:'时间片用完引发的转换是？', opts:['运行→阻塞', '运行→就绪', '就绪→运行', '阻塞→就绪'], ans:1, exp:'被剥夺 CPU（被动）→ 回就绪队尾。'}
]

},

'2-2': {
title: '进程控制与进程通信',
secs: [
['why', '为什么学这一节',
`<p>进程怎么创建（fork）、怎么通信？<b>进程通信三方式（管道、消息传递、共享内存）</b>是选择题常客；"fork 执行细节"是 UNIX 风格的经典题。</p>`],
['think', '直观理解',
`<p><b>进程控制 = 通过原语原子地改变进程状态和相关数据结构。</b>原语执行必须具有不可分割性；在单处理机内核中可短暂屏蔽中断来保护临界更新，多处理机还需要原子指令、锁等同步机制，不能把所有原语都简单等同于“关中断”。</p>
<blockquote><b>三类常见通信方式：</b>① <b>共享存储</b>：多个进程映射同一存储区，数据交换开销低，但同步和互斥要另行保证；② <b>消息传递</b>：用格式化消息直接发送给进程，或经信箱间接通信；③ <b>管道</b>：先进先出的字节流。传统匿名管道通常用于有亲缘关系的进程，并按单向通道理解；有名管道和具体系统规则另看题设。</blockquote>
<p><b>信号量</b>主要是同步与互斥机制，也可借助共享状态传递少量事件信息；大批数据通常仍由共享内存、管道或消息机制承载。</p>`],
['def', '要点',
`<p><b>① fork 语义：</b>调用一次返回两次（父得子 PID、子得 0）；写时复制 COW 提高效率。</p>
<p><b>② 线程通信</b>更简单：同一进程内线程共享全局变量（需同步）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（管道）关于管道通信，正确的是？</div>
<p>管道是<b>半双工</b>（要双向需两个管道）；数据<b>无结构字节流</b>；管道写满时写进程阻塞——三条都是正确说法。</p></div>`],
['warn', '易错点',
`<p>• 共享内存建立映射后可直接读写共享区域，通常适合本机进程间的大批量数据交换，但<b>不自动提供同步互斥</b>，仍需信号量等机制。</p>
<p>• 消息传递可直接（send/receive 指定进程）或间接（信箱）。</p>`]
],
quiz: [
{id:'q1', q:'速度最快的进程通信方式是？', opts:['管道', '消息队列', '共享内存', 'socket'], ans:2, exp:'共享内存零拷贝直达；代价是需自己同步。'},
{id:'q2', q:'管道通信的方向特性是？', opts:['全双工', '半双工', '只能读', '只能写'], ans:1, exp:'一根管道一个方向，双向要两根。'}
]

},

'2-3': {
title: '处理机调度与调度算法',
secs: [
['why', '为什么学这一节',
`<p>408 计算题大户之一：<b>调度算法的周转时间/带权周转时间计算</b>（FCFS/SJF/HRRN/RR/多级反馈队列），几乎年年一道大题或选择。</p>`],
['think', '直观理解',
`<p><b>三层调度：</b>高级调度决定哪些后备作业进入内存；中级调度把暂时不能运行的进程换出或换入；低级调度从就绪队列选择进程占用 CPU。</p>
<blockquote><b>五类算法：</b>① <b>FCFS</b> 按到达先后服务，简单但可能出现短作业等待长作业的护航效应；② <b>SJF</b> 选择估计服务时间最短者，在所有作业同时到达、服务时间已知等模型下使平均等待/周转时间最小，但长作业可能饥饿；③ <b>HRRN</b> 的响应比为“（等待时间+要求服务时间）/要求服务时间”，通常是非抢占式；④ <b>RR</b> 按时间片轮转，时间片过大趋近 FCFS，过小会增大切换开销；⑤ <b>多级反馈队列</b> 用多队列、不同优先级和时间片兼顾响应与吞吐，具体规则以题设为准。</blockquote>`],
['def', '计算模板',
`<p><b>周转时间 = 完成时间 - 到达时间；带权周转 = 周转/运行时间。</b>目标：平均周转/带权周转最小。</p>
<p><b>例（FCFS vs SJF）：</b>作业 P1(到达0,运行7)、P2(2,4)、P3(4,1)、P5(5,4)。</p>
<p>• FCFS 顺序 P1→P2→P3→P5：完成 7,11,12,16；周转 7,9,8,11；平均 8.75。</p>
<p>• SJF：P1(0-7)→P3(7-8)→P2(8-12)→P5(12-16)：周转 7,6,10,11，平均 <b>8.5</b>（更优 ✓）。</p>
<p class="muted">大题套路：列表格（到达/运行/开始/完成/周转/带权），按算法逐行填。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（RR）时间片为 2，忽略切换开销；P1(到达0,运行5)、P2(到达1,运行3)、P3(到达2,运行4)。约定时刻 2 到达的 P3 先入队，再把时间片用完的 P1 放到队尾。</div>
<p>按队列逐段模拟：<b>0-2 P1，2-4 P2，4-6 P3，6-8 P1，8-9 P2，9-11 P3，11-12 P1</b>。</p>
<p>P1、P2、P3 的完成时刻分别为 12、9、11；周转时间分别为 12、8、9。若题目把“边界时刻新到达进程”和“刚用完时间片进程”的入队顺序规定得不同，执行序列也会改变，所以必须写明约定。</p></div>`],
['warn', '易错点',
`<p>• SJF 的"短"按<b>运行时间</b>（作业）或"下一 CPU 区间"（抢占式 SRTF）算，分清作业调度与进程调度版本。</p>
<p>• RR 是<b>抢占式</b>；FCFS/SJF/HRRN 默认非抢占（HRRN 有抢占版）。</p>
<p>• 交互式系统用 RR/多级反馈队列；批处理用 FCFS/SJF。</p>`]
],
quiz: [
{id:'q1', q:'对短作业最有利、平均周转时间最优的非抢占算法是？', opts:['FCFS', 'SJF', 'RR', '优先级'], ans:1, exp:'SJF 数学上平均周转最优，缺点是长作业饥饿。'},
{id:'q2', q:'高响应比优先算法中，响应比 = ？', opts:['运行时间/等待时间', '(等待+运行)/运行', '到达时间/运行时间', '运行-等待'], ans:1, exp:'等待越久响应比越大：动态兼顾长短作业。'}
]

},

'2-4': {
title: '同步与互斥：信号量机制',
secs: [
['why', '为什么学这一节',
`<p>本章皇冠考点：<b>PV 操作解决同步互斥</b>，生产者-消费者、读者-写者、哲学家进餐三大经典模型 + 手写伪代码是大题常客。</p>`],
['think', '直观理解',
`<p><b>互斥 = 抢一个厕所（一次一人）；同步 = 交接班（A 先 B 后的协作次序）。</b></p>
<blockquote><b>信号量 S 与 PV：</b>P(S)：S 减 1，若 S&lt;0 则阻塞自己入等待队列；V(S)：S 加 1，若 S≤0 则唤醒一个等待者。<br>
<b>互斥模板：</b>semaphore mutex=1；各进程 P(mutex)…临界区…V(mutex)。<br>
<b>同步模板：</b>"先做 A 后做 B" → 信号量 S=0；A 后 V(S)，B 前 P(S)。</blockquote>
<p><b>生产者-消费者（必背）：</b></p>
<pre class="code">semaphore mutex=1, empty=n, full=0;
生产者: P(empty); P(mutex); 放产品; V(mutex); V(full);
消费者: P(full);  P(mutex); 取产品; V(mutex); V(empty);</pre>
<p class="muted"><b>P 顺序不能颠倒！</b>先互斥 P 后同步 P 会死锁（生产者占着 mutex 等 empty）。</p>`],
['def', '经典模型速查',
`<p><b>① 读者-写者：</b>读者可并发（第一个读者锁写者、计数器+rmutex）、写者独占（写优先/读写公平变体）。</p>
<p><b>② 哲学家：</b>5 人 5 筷：解决方案=最多 4 人同时拿、奇偶号先拿不同筷、一次性申请（AND 信号量）。</p>
<p><b>③ 吸烟者/理发师：</b>生产消费变体。</p>
<p><b>④ 管程：</b>把共享数据+操作+同步封装成"对象"，自动互斥（任一时刻仅一进程在管程内）——比 PV 更高级的机制。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（同步设计）S1 完成后才能开始 S2 和 S3；S2、S3 都完成后才能 S4——写信号量方案</div>
<pre class="code">semaphore a=0, b=0, c=0;
S1 的代码尾: V(a); V(b);
S2 开头: P(a);   S3 开头: P(b);
S2、S3 代码尾: 各执行一次 V(c);
S4 开头: P(c); P(c);</pre>
<p class="muted">"前驱图转 PV"：每条边一个信号量，前结点尾 V、后结点头 P。</p></div>`],
['warn', '易错点',
`<p>• 多个同步信号量 + 互斥信号量时，<b>同步 P 在前、互斥 P 在后</b>，否则可能死锁。</p>
<p>• V 操作次序不影响正确性（只影响等待时间）。</p>
<p>• S 初值含义：互斥初值 1；同步初值=资源数（empty=n）。</p>`]
],
quiz: [
{id:'q1', q:'信号量 S 初值为 3，经过 5 次 P、2 次 V 后 S = ？', opts:['0', '-1', '1', '3'], ans:0, exp:'3-5+2 = 0，无进程阻塞（S≥0）。'},
{id:'q2', q:'生产者-消费者问题中，若把 P(mutex) 放在 P(empty) 之前，可能发生？', opts:['正常运行', '生产者拿锁后等空缓冲，消费者拿不到锁 → 死锁', '缓冲区溢出', '无影响'], ans:1, exp:'锁内等待是死锁经典成因：P 序反了。'}
]

},

'2-5': {
title: '死锁：预防、避免与检测',
secs: [
['why', '为什么学这一节',
`<p><b>死锁四条件、银行家算法的安全性判定</b>是必考计算题；资源分配图的化简与死锁定理也常出。套公式题，练熟即满分。</p>`],
['think', '直观理解',
`<p><b>死锁 = 一群人互相等对方手里的筷子。</b>四个必要条件缺一不可：<b>互斥、不可剥夺、请求并保持、循环等待</b>——破坏任何一个即可预防（资源一次性分配破坏"请求保持"；资源有序分配破坏"循环等待"）。</p>
<p><b>资源分配图：</b>在每种资源只有一个实例时，图中存在环是死锁的充分必要条件；当一种资源可有多个实例时，“有环”本身只表示可能死锁。资源分配图化简或死锁检测算法还要结合可用实例数判断，不能仅看环。</p>`],
['def', '银行家算法手算模板',
`<p>数据：Available 向量、各进程 Max、Allocation，且 Need = Max - Allocation。</p>
<p><b>安全性流程：</b>令 Work=Available；反复找尚未标记且 Needᵢ≤Work 的进程 i，假定它完成并令 Work+=Allocationᵢ。全部进程都能标记则存在安全序列，否则状态不安全。</p>
<p><b>判断某请求能否立即分配：</b>先确认 Requestᵢ≤Needᵢ 且 Requestᵢ≤Available，再试探更新 Available、Allocation 和 Need，最后运行安全性算法；安全才正式分配，否则回滚并让进程等待。</p>
<p><b>性质：</b>安全状态一定不存在死锁；不安全状态表示系统已无法保证所有进程都能按声明的最大需求完成，但<b>不等于当前已经死锁</b>。银行家算法还要求进程预先声明最大需求，资源实例可计数且模型允许进程完成后释放资源。</p>
<p><b>死锁 vs 饥饿 vs 死循环：</b>死锁是一组进程永久等待组内其他进程才能引发的事件；饥饿是进程长期得不到所需资源或调度机会；死循环是程序控制流不退出，三者不能混同。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（安全性）Available=(3,3,2)；P1 Need(1,0,2) Alloc(2,0,0)；P2 Need(6,1,2) Alloc(3,0,2)？</div>
<p>P1：Need(1,0,2) ≤ (3,3,2) ✓ → 完成，Available=(5,3,2)；P2：Need(6,1,2) ≤ (5,3,2)？6&gt;5 ✗ 卡住。序列仅 {P1} 不含全部 → <b>不安全</b>（拒绝分配）。</p></div>`],
['warn', '易错点',
`<p>• 四条件是<b>必要</b>条件不是充分；预防死锁只需破坏其一。</p>
<p>• 银行家算法问"系统是否安全"与"某请求能否立即满足"是两问，后者先试探再判定。</p>
<p>• 资源有序分配策略对"资源申请顺序固定"的系统有效。</p>`]
],
quiz: [
{id:'q1', q:'死锁的四个必要条件不包括？', opts:['互斥', '不可剥夺', '循环等待', '抢占调度'], ans:3, exp:'四个条件：互斥、请求保持、不可剥夺、循环等待。'},
{id:'q2', q:'破坏"请求并保持"条件的死锁预防策略是？', opts:['资源一次性分配', '资源有序分配', '银行家算法', '剥夺资源'], ans:0, exp:'运行前一次性申请全部资源，不占有再申请。'}
]

}

}
};
