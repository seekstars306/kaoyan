/* ===== 408 操作系统 · 第3章 内存管理 ===== */
window.CONTENT = window.CONTENT || {};
window.CONTENT['cs408/os/ch03'] = {

lessons: {

'3-1': {
title: '内存管理概述与连续分配方式',
secs: [
['why', '为什么学这一节',
`<p>内存管理的任务：分配回收、地址转换、保护扩充。<b>动态分区分配算法（首次/最佳/最坏适应）</b>与碎片概念是本节考点。</p>`],
['think', '直观理解',
`<p><b>程序的地址旅程：</b>编译（逻辑地址）→ 链接 → 装入（重定位成物理地址）。<b>静态重定位</b>（装入时一次改好，不能移动）vs <b>动态重定位</b>（运行时硬件地址翻译，可移动——虚存的基础）。</p>
<blockquote><b>连续分配演化：</b>单一连续 → 固定分区（内部碎片）→ 动态分区（外部碎片）→ 压缩/拼接缓解 → 离散分配（分页）根治。<br>
<b>动态分区算法：</b><b>首次适应</b>（从头找第一块够的，综合最优）、<b>最佳适应</b>（找最小够用的，产生最多小碎片）、<b>最坏适应</b>（找最大块，剩余仍可用）。</blockquote>`],
['def', '要点',
`<p><b>① 内部碎片</b>（分给你的用不完：固定分区/分页最后一页）vs <b>外部碎片</b>（零散空闲装不下：动态分区/分段）。</p>
<p><b>② 动态分区数据结构：</b>空闲分区表/链；回收时注意相邻空闲区<b>合并</b>。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（算法对比）空闲区 100K、500K、200K、300K、600K，作业 212K 用不同算法放置？</div>
<p>首次适应 → 500K（第一个够的）；最佳 → 300K；最坏 → 600K。三算法答案不同——就考这个分辨力。</p></div>`],
['warn', '易错点',
`<p>• 固定分区产生<b>内部</b>碎片，动态分区产生<b>外部</b>碎片，分页产生内部碎片（平均半页），分段外部碎片。</p>
<p>• 首次适应通常查找开销较低，并可能在高地址保留较大的空闲区，但实际效果取决于请求序列、空闲链组织和合并策略，不能无条件称为性能最好。</p>`]
],
quiz: [
{id:'q1', q:'动态分区分配中"最佳适应"算法的特点是？', opts:['优先用低地址', '挑最小的满足要求的空闲区，易产生大量小碎片', '挑最大的空闲区', '随机'], ans:1, exp:'"最佳"名字骗人：留下的碎片最小最多。'}
]

},

'3-2': {
title: '分页与分段存储管理',
secs: [
['why', '为什么学这一节',
`<p>离散分配的两大流派。<b>页式地址转换（页表+快表）与有效访问时间计算</b>是重灾区；两级页表、页的共享保护也常出。</p>`],
['think', '直观理解',
`<p><b>分页 = 把内存切小块（页框），把进程切同样大小（页），任意装。</b>逻辑地址 = 页号 P + 页内偏移 W；查页表得页框号 F，物理地址 = F×页长 + W。</p>
<blockquote><b>有快表(TLB)的有效访问时间：</b>TLB 命中：t_TLB + t_mem；未命中：t_TLB + t_mem(查页表) + t_mem(取数)。按命中率加权求平均——标准计算题。<br>
<b>两级页表：</b>页表再分页，页目录寄存器 → 二级页表 → 页框，解决"页表太大连续存放"问题。</blockquote>
<p><b>分段 = 按逻辑意义切（代码段/数据段/栈段）。</b>段表项 = 基址+段长；地址转换要查<b>越界检查</b>（分段特有）。分页利于内存利用（一维地址），分段利于共享保护（二维地址）。</p>`],
['def', '计算模板',
`<p><b>① 位数拆分：</b>逻辑地址 32 位、页 4KB → 页号 20 位、偏移 12 位。</p>
<p><b>② 有效访问时间（TLB 命中率 h、TLB 查询时间 ε、一次主存访问时间 t）：</b>若先查 TLB，命中后访存一次；未命中时页表项在主存且不缺页，则访存两次：</p>
$$ EAT = h(\\varepsilon + t) + (1-h)(\\varepsilon + 2t) $$
<p class="muted">若题目规定 TLB 与 Cache 并行、页表项也可能命中 Cache，或需计入缺页，则必须按题设重新画访问路径，不能直接套此式。</p>
<p><b>③ 段页式：</b>不使用 TLB 且段表、页表均在主存时，地址转换通常要查段表、查页表、再访问目标数据，共三次访存。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例 1（地址转换）页大小 1KB，逻辑地址 2500（十进制）的页号与偏移？</div>
<p>2500 = 2×1024+452，所以页号为 <b>2</b>、页内偏移为 <b>452</b>。若页表项 P2 给出的页框号为 7，则物理地址 = 7×1024+452 = <b>7620</b>。</p></div>
<div class="ex-box"><div class="ex-t">例 2（分段越界）段表：段0(基址40K,长108K)、段1(基址360K,长40K)。判断逻辑地址 (段2,100) 与 (段1,50K)</div>
<p>段表中没有段 2，所以 (段2,100) 发生<b>段号越界</b>。(段1,50K) 的段内偏移 50K 不小于段长 40K，也发生<b>段内越界</b>；合法偏移范围应为 0～40K-1。</p></div>`],
['warn', '易错点',
`<p>• 页号/偏移拆分用<b>除法与取模</b>，注意页长 2 的幂才能位拼接。</p>
<p>• 分页对用户透明（一维逻辑地址），分段用户可见（二维 段:偏移）——共享保护选分段。</p>
<p>• 多级页表"下级页表可以离散存放"，页目录本身常驻。</p>`]
],
quiz: [
{id:'q1', q:'页大小 2KB，逻辑地址 5000 的页内偏移是？', opts:['904', '2', '976', '1000'], ans:0, exp:'5000 mod 2048 = 904，页号 = 5000 div 2048 = 2。'},
{id:'q2', q:'与分页相比，分段便于实现信息共享保护的原因是？', opts:['段长固定', '段是完整逻辑单位（如一段代码）', '分段无碎片', '分段更快'], ans:1, exp:'按逻辑意义分段，整段共享一份副本即可。'}
]

},

'3-3': {
title: '虚拟内存与页面置换算法',
secs: [
['why', '为什么学这一节',
`<p>虚存 = "内存+磁盘 合体成大内存"的魔法。<b>请求分页、缺页中断、OPT/FIFO/LRU/CLOCK 置换、Belady 异常、抖动</b>——408 计算与概念的双料重镇。</p>`],
['think', '直观理解',
`<p><b>虚拟内存三项基础：</b>请求调页（访问时发现不在内存才调入）、页面置换（没有空闲页框时选择换出页）和局部性原理。</p>
<blockquote><b>置换算法手算（给访问串、页框数，数缺页）：</b><br>• <b>OPT 最佳</b>：淘汰以后最长时间不再访问的页，理论最优但无法在线预知未来；<br>• <b>FIFO</b>：淘汰最早调入的页，可能出现 <b>Belady 异常</b>；<br>• <b>LRU</b>：淘汰过去最长时间未使用的页，属于栈算法，不会出现 Belady 异常，但精确实现开销较大；<br>• <b>基本 CLOCK</b>：循环检查访问位，访问位为 1 就清零并跳过，为 0 才淘汰，是对 LRU 的近似；改进 CLOCK 还结合修改位。<b>NRU</b> 则按访问位和修改位把页面分类后从低优先级类别选择，不应与 CLOCK 当成同一个算法。</blockquote>`],
['def', '手算模板与抖动',
`<p><b>例（LRU 演示）访问串 1,2,3,4,1,2,5，3 个页框：</b></p>
<p>1缺→[1]；2缺→[1,2]；3缺→[1,2,3]；4缺淘汰1→[2,3,4]；1缺淘汰2→[3,4,1]；2缺淘汰3→[4,1,2]；5缺淘汰4→[1,2,5]。共 <b>7 次缺页</b>。</p>
<p><b>抖动（颠簸）：</b>刚换出的页马上又要用 → 频繁缺页，CPU 利用率骤降。原因：进程分配页框太少 &lt; 工作集；解决：按工作集模型分配、降低多道程序度。</p>
<p><b>缺页率计算：</b>缺页次数/访问次数； Effective Access Time 类似 Cache 公式（命中率×内存 + 缺页率×磁盘）。</p>`],
['ex', '例题精讲',
`<div class="ex-box"><div class="ex-t">例（Belady）访问串 1,2,3,4,1,2,5,1,2,3,4,5，FIFO 下 3 帧与 4 帧缺页数？</div>
<p>3 帧：9 次缺页；4 帧：<b>10 次</b>——页框多了缺页反而多，这就是 Belady 异常（LRU/OPT 无此现象）。</p></div>`],
['warn', '易错点',
`<p>• "缺页必然引发页面置换"错误——内存有空闲框时只调入不置换。</p>
<p>• LRU 的"最近未使用"看<b>过去</b>，OPT 看<b>未来</b>，别混。</p>
<p>• 首次装入也算缺页（教材约定），数缺页时别漏。</p>`]
],
quiz: [
{id:'q1', q:'下列页面置换算法中，经典反例可证明会出现 Belady 异常的是？', opts:['OPT', 'LRU', 'FIFO', '基本 CLOCK'], ans:2, exp:'FIFO 可出现页框增加而缺页次数反增的 Belady 异常；OPT 和 LRU 属于栈算法，不会出现。基本 CLOCK 在本题中不是该经典结论的唯一指定答案。'},
{id:'q2', q:'LRU 算法淘汰的是？', opts:['最早进入的页', '未来最久不用的页', '最近最久未使用的页', '访问位为 0 的随机一页'], ans:2, exp:'"过去"视角：最久未被访问的先走。'}
]

}

}
};
