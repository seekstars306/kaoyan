# 内容质量审查与验证报告（2026-09-11）

## 结论

本轮已按课程顺序完整审查并修订全部 **53 个内容文件、204 个课时、480 道随堂题**：数学 19 个文件 / 84 课时 / 230 题，英语 8 个文件 / 37 课时 / 79 题，408 共 26 个文件 / 83 课时 / 171 题。

在首轮零基础知识链重构与全学科例题复核的基础上，本轮又执行了第二轮全量题库符号展开与选项互斥性扫描，针对性清除了 5 处多解与等价选项隐患（乘积导数等价选项、隐函数求导等价选项、轮换对称积分多解、英式长短元音与教材分类重叠、定语从句关系代词边界），并将其全部固化为自动化回归断言。

仓库现已提供无网络依赖的校验脚本 [`scripts/validate-content.js`](scripts/validate-content.js)。截至本报告更新时，64 个 JavaScript 文件的语法检查，以及课程树与内容映射、结构、题目基本约束、HTML 标签、公式分隔符、源文件转义、运行时控制字符、本地 KaTeX 0.16.11 严格解析和高风险回归断言均 100% 通过。报告不再使用“人工核对且零错误”一类无法独立复现的结论。

## 可复现命令

在仓库根目录运行：

```bash
find . -type f -name '*.js' -not -path './.git/*' -print0 | sort -z | xargs -0 -n1 node --check
node scripts/validate-content.js
git diff --check
```

当前内容校验输出：

```text
content-validation-ok files=53 chapters=53 lessons=204 questions=480 formulas=4577 warnings=0
```

脚本校验内容包括：

- 课程树与内容文件双向一致，且总量固定为 53 章、204 课时、480 题；
- 课时内部标题与课程树一致，section 类型和字段结构合法；
- Quiz ID 在单课内唯一，选项为 2～4 个，答案下标合法，题干、选项和解析非空；
- 精确重复选项报错，并对规范化后疑似等价选项给出警告；
- 内容 HTML 标签正确嵌套，公式分隔符配对；
- JavaScript 字符串内不存在单反斜杠命令或非法控制字符；
- 4,577 个运行时公式全部由仓库本地 KaTeX 0.16.11 以 `throwOnError: true`、`strict: 'error'` 解析；
- 数学因式分解教学链、三重积分、概率条件、英语音标、C 整数除法、malloc、KMP、循环链表、TOP-K、轮转调度、SCAN/LOOK、TCP ACK 与 cwnd 以及二轮题库去重均有回归断言。

## 第三轮脉络复查（2026-09-11）

针对「知识点—例题—知识脉络」做了第三遍系统性检查：

- **未讲先用扫描**：按课程顺序建立 40 余个核心术语的"首次定义位置 vs 例题/测验使用位置"对照，逐条人工甄别 11 个报警（多数为子串误报，如"平方差公式"命中"方差"）。唯一实质问题已修：一元微分课时提前使用"偏导"结论，已补"偏导数是第 6 章内容"的显式标记。
- **数值例题独立重算**：全部 124 个含计算的例题逐题重做（高数 ch00–ch08、线代 lach01–06、数一 sych01–04、408 四门），包括复合函数定义域、莱布尼茨 n 阶导、轮换对称积分、KMP nextval、哈夫曼 WPL、IEEE 754 编码、低位交叉存取、LR 旋转、子网分片、两阶段缓冲等——计算结果全部正确。
- **脉络结构检查**：204 课全部满足"定义先于例题"的 section 顺序；测验题干未引用更晚章节的方法。
- **修复的 5 处表述缺陷**（均已固化为回归断言）：
  1. `english/m0` station 重音演示把重音标在第二音节（`sta TION`），与它自己给出的音标 `/ˈsteɪʃn/` 及"重音在 -tion 前一拍"规则矛盾——改为标重第一音节并加注说明；
  2. `english/m7` 图表例句数据为 20 亿→80 亿，恰为 4 倍，原句 "nearly quadrupling" 数字逻辑错误——改为 exactly quadrupling；
  3. `english/m7` 句型升级例题的"变化点"称原句为 there be 罗列，与原句实际结构不符——重写为准确的合并句法描述；
  4. `cs408/ds/ch06` AVL LR 型旋转结果写"35 在 40 左"，与真实终态（40 为根、35 挂在 30 的右孩子）不符——改写为明确的父子关系；
  5. `math/ch02` 提前引用"偏导"未标注出处章节——补第 6 章标记。

## 主要修订

### 数学

- 重写第 0 章零基础代数链：先讲运算顺序、分配律、展开、合并同类项和二项式相乘，再从 `(x+m)(x+n)` 推出和积法。`x²-5x+6` 现在包含因数对枚举、符号判断、确定 `-2` 和 `-3` 以及重新展开验算。
- 补全根式、分式、复合函数、反函数、极限、中值定理、洛必达、泰勒、隐函数、积分和微分方程的定义域、参数条件、端点与遗漏分支。
- 修订线性代数中的默认数域、秩、解空间、对角化、合同、正定与正交变换条件。
- 修订数一扩展中的空间解析几何、三重积分、曲线曲面积分、级数和概率统计条件，并消除连续型单点概率题的重复正确选项。
- 二轮深审修复：消除 `y=x³ln x` 导数题中 `3x²ln x+x²` 与 `x²(3ln x+1)` 的等价重叠；消除隐函数求导中 `x/y` 与 `2x/2y` 相同干扰项；消除二重积分轮换对称题中 `∬x²dσ` 与 `½∬(x²+y²)dσ` 的数值重合。

### 英语

- 纠正短元音例词与错误 HTML，明确英式/美式音标及传统教学分类的口径。
- 收紧自然拼读、词根词缀、语法、阅读技巧、翻译和写作中的过度绝对化表述。
- 修订例句、题目选项、时态、称呼、落款与字数建议，避免把经验性策略写成硬规则。
- 二轮深审修复：将“长短音对立”题中与本教材元音表重叠的 `dog / door` 改为清晰的非长短对立干扰项 `pen / pan`；修复定语从句中先行词表时间时关系词选项，排除口语中 that 兼容用法带来的歧义。

### 408

- 数据结构：按 C 标准修订整数除法、`malloc`、边界和代码语言混用；统一 KMP `next` 约定；修复循环链表答案、树遍历、折半查找、B 树、哈希、TOP-K 和排序复杂度问题。
- 计算机组成原理：修订计算机代际、IEEE 754、Cache/TLB、RISC/CISC、数据通路、流水线、总线、中断异常和 DMA 的条件及数值。
- 操作系统：修复进程状态转换、RR 序列、资源分配图、银行家算法、TLB 路径、分段越界、页面置换、FAT、SCAN/LOOK、缓冲和磁盘时间模型。
- 计算机网络：修订分组流水时延、奈奎斯特/香农条件、曼彻斯特与 NRZI、CRC、窗口条件、以太网帧、CIDR/NAT、ICMP、IPv6、TCP ACK/Reno/cwnd、DNS/FTP/SMTP/MIME 和 HTTP 模型。

## 应用错误可见性与版本标识

- [`assets/js/app.js`](assets/js/app.js) 不再把章节脚本加载或执行异常静默转换成 `null`。课时页和章节测验页现在会显示具体失败文件与错误信息，并提示运行仓库校验脚本，避免源文件异常被泛化为“内容缺失”。
- 首页主仪表盘标题及全局页脚统一加入版本号标识（当前 `v1.0.3`）。

## 浏览器验收（2026-09-11，真实浏览器）

通过 ZCode 内置浏览器（Chromium，`http://127.0.0.1` 本地服务）完成真实 GUI 验收，方法为 DOM 快照 + 只读页面检查 + 实际点击/输入交互；截图已存档，但受当前会话模型能力限制未做人工目检，视觉判断以 DOM 结构与几何检查为准：

- **全量课时遍历**：204 个课时页面逐一加载，均无“章节加载失败”提示、无占位文案、无 KaTeX 错误节点（`.katex-error` = 0），每课正文区块与随堂测验齐全。
- **全量章节测验遍历**：53 个章节测验页逐一加载，题目总数合计 480，与门禁一致；“来自：××小节”来源标注正常。
- **测验交互**：空答提交有提示；作答提交判分（3/3 与 2/3 两种路径）、正确/错误标记、解析展示、重做按钮均验证通过；重做路径再次判分正常。
- **错题本闭环**：答错自动收录并按科目过滤；重做答对显示“已从错题本移除”并自动清空；重做答错显示正确答案并保留。此环节发现并修复了一个真实功能缺陷（见下）。
- **响应式**：390×844 移动视口下底部标签栏正常显示、页面无横向溢出；1280×720 桌面视口布局正常。
- **功能页抽查**：背单词、错题本、学习计划、设置四个页面均正常渲染，无加载失败。
- **控制台日志**：本浏览器运行时未提供只读 console 读取能力，无法收集 console 记录；以页面可见错误表现（加载失败提示、KaTeX 错误节点、空内容）作为替代证据，均未出现。

### 浏览器验收发现并修复的缺陷

错题本“提交重做”在真实浏览器中完全无响应。原因：判分代码用未加引号的 CSS 属性选择器 `input[name=w_<qid>]:checked` 查询作答，而 qid 含 `/`（如 `math/ch00/0-1/q1`），该写法在 `querySelector` 中抛出 `SyntaxError`，判分中断且无任何用户可见反馈。静态审查无法发现此类缺陷（`node --check` 与内容校验均不覆盖）。修复为加引号的 `'input[name="w_<qid>"]:checked'`，并同步加固了课时测验判分处的同类写法（`q<序号>` 此前恰好合法）。修复后重做答对/答错两条路径均实测通过。

## 验证边界

- 程序化门禁能证明结构、转义、公式可解析和列出的回归事实，但不能形式化证明全部自然语言陈述或 480 道题的数学语义永远无误。
- 本轮逐课审查独立重算了已识别的计算题并修复了发现的问题；后续内容改动应重新运行上述命令。
- 英语阅读、翻译与写作中的教学短例是自编材料，不应标为历年真题原文。
